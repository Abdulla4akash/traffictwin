"""TrafficTwin data-platform v1 surfaces (owner directive, 30 July 2026).

Each platform slice lives in its own module and is imported by full path;
this package deliberately re-exports nothing. Design documents live in
``docs/platform/``.
"""
