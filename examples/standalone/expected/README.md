# Expected Standalone Outputs

Generated standalone workspaces are not committed here. Use:

```bash
traffictwin demo initialise .demo
```

Expected outputs are written under `.demo/exports/` and `.demo/reports/`.
