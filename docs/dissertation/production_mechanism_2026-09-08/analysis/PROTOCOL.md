# Production-domain mechanism protocol

Declared 8 September 2026 before execution of additional kernel inputs. Base: TrafficTwin `1b6a8555c27901ec95ad8b123c413453ecdb5620`. All previous artifacts remain unchanged. No deleted-output search, recovery, historical rerun, full evaluator, actor, SUMO or campaign is part of this work.

Question: when does the simultaneous fixed-target map, starting from fixed eligibility, equal the causal fixed-target scan? Investigate exact arithmetic first; inspect float32 separately. The proposed exact bound is min(n, 2d−1), for n eligible candidates and d distinct deadlines per fixed destination. The proof depends on nonnegative work, exclusive earlier-candidate dependencies, shared capacity per destination and fixed targets/eligibility. It does not concern endogenous per-task target selection.

Maximum **800 additional small inputs**, counting attempted minimisation/translation inputs conservatively even if repeated. Same input evaluated by exact reference, existing scalar reference and production helper counts once. No outcome-driven expansion:

1. **128 exact-model fixtures:** eight length-eight deadline orders (all100, all500, alternating100/500, reversed alternating, grouped100then500, grouped500then100, ascending three thresholds1/41/81, descending500/100/20); four nonnegative service patterns (allzero, all40, 0/20 alternating, 1..8); four initial backlog states0/20/100/500. Capacity and initial count cycle deterministically through empty, one spare place, four places and full; alternating fixtures use one common target or two interleaved fixed targets. These general fixtures include zero work and non-production thresholds; they test assumptions/bounds, not production frequency.
2. **324 task-parameter-compatible fixtures:** six length24 type orders (all type1, all type2, all type3, 1/2/3 cycle, 1/2 alternation, 3/2/1 cycle), noise multipliers0.91/1/1.09, initial workloads0/90/490, three fixed-target patterns (one RSU, two interleaved destinations, target1 among three RSUs), and all-eligible versus every fourth ineligible. Task-count capacity6220, initial count0/4/20 respectively. Services use each type's frozen formula and its own deadline100/500/100; the service multiplier remains1. These inputs satisfy task/range/configuration constraints, but their queue/RNG/traffic reachability is not asserted.
3. **219 numerical-boundary fixtures:** n=2/3/8, three type orders (type3 prefix then type1; type3 prefix then type2; type1 prefix then type2), three multipliers and seven adjacent float32 backlog values centred on the last candidate's exact threshold minus prefix service:189 cases. Another21 cases use n=24, type3 prefix then type1. Nine one-task completion boundaries use each type and backlog one float32 value below/equal/above deadline minus service. All services are rounded to float32 before being supplied identically to every arm. No physical RNG sampling is performed.
4. **At most120 adverse-case inputs:** select the first adverse B→C case in the already saved order. Re-evaluate that input; greedily reduce substep count and delete candidate positions (shared by all arms). Limit reduction to80 trials including the original. Up to40 deterministic coupling attempts map the retained short/long synthetic deadline classes to production100/500 and each service to the nearest allowable range for that deadline (ties choose lower type index); check original and reduced forms, noise0.91/1/1.09 and capacity original/6220. Retain failure to transfer. B always replays A's complete target sequence and is target-conditioned.
5. **At most9 numerical minimisation inputs:** if production/exact masks disagree, retain the first case and attempt single-candidate deletion within this allowance. No tolerance changes or further search.

The exact reference uses rational values of the supplied inputs, not rounded summation. The unchanged previous Python reference supplies a separate scalar comparison. The production `seq_offsets` body is extracted without editing; its service/deadline provider supplies explicit arrays. A diagnostic wrapper copies only its prefix/rank arithmetic to expose M0–M4; returned M3/offset/label fields must agree with the unchanged helper. That trace is reconstructed by an instrumented arithmetic mirror, not a saved production intermediate. The pure causal module is called unchanged for single-target scans; multiple targets use disjoint subsequences. No scientific runtime packages change.

**Predeclared numerical rules:** masks, targets, ranks and labels require exact equality; no tolerance excuses a discrete disagreement. Report all such disagreements. Float32 offset/work differences are reported in milliseconds, with diagnostic allclose threshold abs≤0.001 ms and rel=0 (not an admission tolerance or physical-error bound). Near-threshold signs and one-ULP effects are retained even below that diagnostic threshold. Compare exact rational arithmetic on float32 inputs, the old Python-scalar reference, the float32 causal helper and the unchanged simultaneous helper separately. Fix CPU/JAX0.4.30, x64 disabled, and record versions/hashes. No claim of universal float32 equivalence follows from passing fixtures.

Reuse the existing authenticated19-run audit and23paired joins; do not rerun them. Verify source/result hashes against1b6a855, extract every primary morning ingress→per-task draw, and reconcile category gains/losses with the saved net counts. No raw-array input is required. The proof and diagnostics are retrospective AI-assisted work, not historical experimental motivation or unaided student work.
