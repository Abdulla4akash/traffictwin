# Reserved numerical follow-up, declared before execution

The initial671 inputs are complete. Their result hash and original protocol hash are retained. One near-threshold fixture alternates after three passes, but its default Q=1 with W≈82.323 ms is not justified as a reachable production queue. Task/deadline compatibility alone is insufficient to assert full-state reachability.

Use at most **nine** additional inputs, replacing the original numerical-minimisation allowance; the total ceiling remains800. First, re-evaluate that already recorded unstable case with Q=4. This keeps capacity nonbinding and makes W/Q≈20.581 ms compatible with four type1 services. Then evaluate one four-task prefix from W=Q=0, each service equal to the saved W/4 and deadline100; its returned offset/admitted-work fields test this local queue construction. The services fall inside the production type1 range. This establishes only compatibility with two admission substeps under explicit service arrays, not an actor/traffic/PRNG-reachable full trajectory or an observed case. Do not sample keys to seek those exact values.

Use the remaining at most seven inputs for single-candidate deletion of the first near-threshold production/exact discrepancy (the two-task near_000_+0 case). Stop when all its possible single deletions have been checked or the budget is used. Do not tune numerical tolerances. Preserve original fixtures and the correction in interpretation; do not replace their original Q fields.

The adverse-case allowance remains120 inputs and does not grow. This amendment responds to the numerical-state assumption, not to a desired success rate. No additional full-system work is permitted.

## Final adverse dimension check, declared after the first transfers

The two-RSU nominal transfer gives B8 versus C7, but reduced infrastructure is not the actual nine-/ten-RSU configuration. Before describing its scope, evaluate exactly12 final transfers: original/reduced candidate vectors × noise0.91/1/1.09 × R9/10, always K5, C6220, all-zero initial queues and identical repeated vectors in each arm. These consume the existing120-input adverse allowance (27 used so far); total ceiling remains800. Do not add more cases if no adverse result survives. This is a small synthetic admission batch using production task/range/configuration parameters, not a full traffic simulation or observed fleet replication.

## Reviewer-triggered shape check, declared before execution

The separate critique notes that prefix rounding can depend on padded shape. Use four of the five unused numerical follow-up inputs (the numerical allowance remains nine): pad the already selected Q4 eight-candidate alternating fixture to N215/R9 and N2488/R10, with its active block at the beginning or end. Other entries are inactive with zero reserved work; keep the eight services/deadlines, W/Q and fixed target0 unchanged. Other RSU workloads/counts can be zero because this is fixed-target admission, not target selection. Call the unchanged helper and its existing diagnostic mirror; compare active entries with the exact eight-candidate result. Do not search further if the discrepancy vanishes. This tests actual array dimensions, not fleet/actor/RNG reachability or frequency. Total new inputs will be718, below800.

## Final target-consistency fixture

Use the last numerical input (nine total,719 overall) to distinguish a fixed-target arithmetic fixture from common-target selection. A target with W≈82.323 would not be selected while other RSUs are empty. Prepend its four explicit type1 services to the existing eight-task block and start all workloads/counts at zero, N215/R9, capacity6220, all twelve active candidates assigned to target0. This makes target0 the actual lowest-index entry argmin and places the workload inside the prefix rather than in base W. Keep every service and deadline unchanged. Check the resulting masks/offsets with the unchanged helper and rational reference. Stop regardless of outcome; no additional parameter tuning or cases.
