# Execution authorisation amendment — 8 September 2026

The owner explicitly authorised this additional morning study in the current
task: EXECUTE_NEW_CONFIRMATION=true, bounded short qualification followed by
at most 32 full attempts. The reviewed baseline is
`c049f00f2247bfbd1196d5e6524de8198fdd2df7`. The original empirical-extension
PROTOCOL.md, SEALED.json and disabled receipts remain unchanged. This directory
will hold an independently bound execution version after qualification; the
authorisation does not itself assert that qualification or execution passed.

Scientific design, estimand, three primary contrasts, Bonferroni Student-t
family, eight pairs (100,200) through (107,207), rotating arm order and fixed
controls are inherited verbatim from the original protocol. Frozen weights
permit endogenous observations and actions; input identities are checked
separately. No outcomes determine seeds, order, sample size or continuation.
The first completed four-arm block is included, not an inspected pilot.

The local CPU runtime is CPython 3.11.15, JAX/JAXlib 0.4.30, NumPy 1.26.4,
float32, x64 disabled. Trace and actor must match the original declared hashes.
The full budget remains 32 attempts including failures, serial only, three
hours per attempt, 50 GiB initially and 20 GiB before each attempt. Failed and
incomplete attempts are retained and never automatically retried. An exclusive
process lock will prevent overlapping runners. Simulation process time,
validation time and timestamps will be recorded separately. No remote storage
destination is approved; local outputs are not an off-machine backup.

Source inspection identifies qualification work rather than a scientific
policy change: the prepared runner hardcodes the full horizon, lacks a launch
lock and separate validation timing, and does not check all recorded proposal,
forwarding or task-count carry relationships. Additive instrumentation is
needed for direct final admission, cyclic pointer transitions and vehicle
service/count accounting. A new versioned evaluator will expose already
computed values without changing their use, arithmetic or random-key schedule.
Three old study arms must agree with unchanged frozen runs on every shared
scientific field. Original imports and supplied repair code remain attributed
to their recorded sources; new instrumentation/validation is AI-assisted work.

This authority applies only to this study. Historical E0/E1/E2 recovery,
other campaigns, training, cluster/paid services, PDF/DOCX work, push, merge,
upload and dissertation submission remain outside scope.

## Qualification plan declared before evaluator execution

Use the previously inspected morning pilot pair fleet=1, evaluator=0, confirmed
in the preserved pilot manifest. Maximum eight short evaluator attempts, no
more than 300 steps each; failures count. Planned order:

| Attempt | Source/arm | Steps | Purpose |
|---|---|---:|---|
| 1 | Frozen ingress_dla | 300 | Unchanged-arm reference |
| 2 | New instrumented ingress_dla | 300 | Full field contract, instrumentation equality |
| 3 | Frozen dla | 300 | Unchanged common-target reference |
| 4 | New instrumented dla | 300 | Three-pass compatibility and accounting |
| 5 | Frozen per_task_dla | 300 | Unchanged causal reference |
| 6 | New instrumented per_task_dla | 300 | Causal compatibility and accounting |
| 7 | New causal_round_robin | 300 | Four-arm controls, pointer progression and carry |
| 8 | New causal_round_robin, fresh process | 150 | Zero-initialised pointer and exact prefix restart |

An encountered defect is preserved with its outputs and may consume a remaining
attempt for a targeted rerun; the ceiling is not increased. Full execution is
withheld unless all required coverage is established within this budget.
Kernel-only pointer tests already completed are reused, not counted as new
fleet evidence. Qualification observations do not enter primary inference.

Before launching any short run, bind its current sources, command, runtime,
inputs and tolerances. Discrete arrays, shared-input arrays, summary integer
counts and old/new scientific-field compatibility are exact. JSON score
reconstruction tolerance remains 1e-6 tasks; per-queue per-second service
conservation 0.01 ms absolute; drain 0.001 ms plus 1e-6 relative. No threshold
is relaxed in response to observed failures. Penalty latency is exactly ten
times deadline and success uses the inclusive recorded-type deadline.

Validate four-arm shared exogenous arrays, report observation/logit/action
differences without forcing equality, and exercise refusal tests using isolated
copies/mocks for a corrupted count, mismatched shared input, missing block
receipt and changed output hash. No original output is corrupted. Missing or
misnamed fields fail qualification. The execution seal will bind the final
validator and successful qualification receipt before any full outcome.
