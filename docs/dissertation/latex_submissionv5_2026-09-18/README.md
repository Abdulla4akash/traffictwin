# TrafficTwin submission version 5 — 18 September 2026

`TrafficTwin_Dissertation.pdf` and `TrafficTwin_Dissertation.tex` are the revised PDF and matching source. The submission copy is `Desktop/SubmissionDiss/submissionv5.pdf`.

Changes from the browser-citation revision: replace the four copyright/IP paragraphs with the current University of Manchester taught-dissertation statement (guidance section 4.1(f)); use the simple displayed label **Word count: 8,386**, without the previous qualification. The count uses the existing reproducible counting convention; no counted research text changed.

The copyright guidance PDF, source diff, word-count receipt and verification results are retained in `submissionv5_checks/`. Build with `./build.sh`. Reproduce the count with `python3 revision_checks/recount.py` (requires markdown-it-py).

The original package and all earlier submission PDFs are preserved. Other inherited check directories describe earlier revisions. The included evidence files and their relative links are unchanged; keep the extracted source package together when following those links.
