# TrafficTwin Assumption Register

Statuses:

- `confirmed`: directly evidenced in the current repository or user brief.
- `inferred`: reasonable working interpretation, but not directly proven.
- `unknown`: not evidenced.
- `contradicted`: existing evidence conflicts with another instruction or assumption.

| Assumption | Evidence | Status | Affected feature | Fallback behaviour |
|---|---|---|---|---|
| `diss/` is the TrafficTwin project root. | Abdulla explicitly confirmed `diss/` as the permanent TrafficTwin project root before Phase 1. | confirmed | File layout, docs, Phase 1 scaffold | Keep all new TrafficTwin files inside `diss/`. |
| The complete v0.4 design has been supplied locally. | The original `diss/AGENTS.md` contained `TrafficTwin - Design Proposal (v0.4)` and was read completely before relocation. | confirmed | Phase 0 planning | Use `docs/traffictwin-design-v0_4.md` as the canonical design source. |
| The canonical design path exists. | The design was copied exactly to `diss/docs/traffictwin-design-v0_4.md`. | confirmed | Agent discovery, documentation traceability | Require agents to read the canonical spec before architectural work. |
| Phase 1 TrafficTwin implementation exists. | `src/traffictwin/`, `pyproject.toml`, examples, and tests were added in Phase 1. | confirmed | Phase 2 foundation | Build Phase 2 on the tested package scaffold. |
| No real data is available in the workspace. | Superseded by Randy's external `TOS Data` package cloned under `external/tos-data` on 2026-07-18. | contradicted | Adapters, metrics, validation | Continue using synthetic fixtures for standalone demos, but use the external package for Phase 6B discovery. |
| Randy's runnable environment is available locally. | The `TOS Data` package contains result artifacts and points to a separate `vec_env` repository; no runnable source, command contract, job script, or checkpoint files are present locally. | unknown | Launcher, adapters, capability manifest | Keep direct launch unavailable and scenario controls unknown. |
| Direct launch is supported. | No CLI/API/script contract is present. | unknown | Scenario Studio run button, launcher | Export seed only; hide or disable direct run. |
| Randy's evaluation-summary CSV schema is known. | `external/tos-data/evals/eval_results_master.csv` and `DATA_DICTIONARY.md` document a 300-row evaluation summary table. | confirmed | Integration discovery, metric reconciliation | Treat as external summary data, not raw canonical task/infrastructure records. |
| SUMO trip records will be available. | Design mentions SUMO trip output as assumed; repo has no SUMO files. | unknown | Journey-Time Lens | Mark trip metrics unavailable unless `trips.csv` is present and valid. |
| Per-RSU queue and utilisation are exported over time. | Design notes queue state may exist but export is unconfirmed. | unknown | Infrastructure metrics, R2 | Compute infrastructure metrics only from valid `infra_state.csv`; otherwise return unavailable. |
| Energy per completed task is available. | Design marks energy logging as assumed/unconfirmed. | unknown | Energy metrics | Do not compute energy metrics unless the column and unit are declared. |
| Task decisions include local, V2I, and V2V. | User brief defines these allowed decisions; Randy's data dictionary maps `veh_action` values 0, 1, 2 to local, V2I, and V2V in per-step arrays. | confirmed | Seed schema, validation, metrics, Randy adapter | Use these labels in synthetic and generic contracts; adapter can map Randy integer labels after scope approval. |
| Unknown fields should remain absent or null. | User brief explicitly requires absent fields not be fabricated. | confirmed | Canonical models, validation, metrics | Preserve nulls; mark dependent metrics unavailable. |
| Deterministic code must compute all metrics. | User brief states numbers come from deterministic code. | confirmed | Metrics engine, rules, UI | Keep LLM out of metric and diagnosis computation. |
| Diagnostic outputs are hypotheses, not proven causes. | User brief and design both require softened causal claims. | confirmed | Rules engine and UI copy | Use `hypothesis` language and include alternatives/missing evidence. |
| Earlier XITS instruction says not to lock design before `MATERIALS COMPLETE`. | `XITS/README.md` and `XITS/research_context.md` contain that gate, but Abdulla explicitly superseded it for implementation purposes with the approved TrafficTwin v0.4 specification. | contradicted | Planning authority | Preserve XITS as historical research material, not an active implementation blocker. |
| The first vertical slice should be synthetic. | No real data or simulator exists locally; user permits synthetic fixtures if no real data exists. | confirmed | Phase 1-4 planning | Build synthetic baseline/variation demonstration and label it synthetic. |
| SQLite is sufficient for Phase 1 registries. | Phase 1 registry uses standard library `sqlite3`; tests verify persistence across registry instances. | confirmed | Metadata registry | Keep SQLite for metadata; defer DuckDB/Parquet until run volume justifies it. |
| Streamlit is the first UI. | User explicitly lists Streamlit as preferred. | confirmed | Phase 4 UI | Keep UI thin over tested library services. |
| FastAPI, React, queues, and cloud infrastructure are out of scope. | User explicitly excludes them unless demonstrated requirement exists. | confirmed | Architecture | Do not add these in the protected vertical slice. |
| The package should target Python 3.11+. | User explicitly specifies Python 3.11+. | confirmed | Packaging and typing | Configure package metadata for Python 3.11 or newer. |
| The local quality-gate interpreter is Python 3.12. | `/opt/homebrew/bin/python3.12` is available; system `python3` is 3.9.6. | confirmed | Tooling, tests | Use Python 3.12 for local validation and declare Python 3.11+ support in packaging. |
| PyYAML is sufficient for Phase 1 seed files. | Phase 1 deterministic seed load/dump tests pass with PyYAML. | confirmed | Seed I/O | Reconsider ruamel.yaml only if comment-preserving edits become a requirement. |
| No unsupported Randy capability defaults to `true`. | Phase 1 tests and smoke assertion check all Randy-environment capability fields. | confirmed | Capability-aware UI and launch controls | Keep direct launch `false` and unconfirmed controls `unknown` until evidence changes. |
| Phase 1 registry stores metadata only. | `storage.registry` stores seed, experiment, and run JSON payloads; no canonical traffic/task tables exist. | confirmed | Storage boundary | Add canonical data storage only after Phase 2 ingestion and validation design is stable. |
| Phase 2 canonical records are in-memory only. | `CanonicalTables` holds lists of Pydantic records; SQLite stores run and bundle metadata, not row data. | confirmed | Storage boundary | Keep row storage deferred until metrics/data-volume evidence justifies it. |
| Generic CSV import is manifest-driven. | `GenericCsvAdapter` uses manifest file declarations, column maps, and units. | confirmed | Adapter boundary | Do not add Randy-specific CSV assumptions to the generic adapter. |
| Directory and ZIP bundles should be equivalent. | Tests compare directory and ZIP validation results. | confirmed | Bundle loading | Preserve ZIP safety checks and deterministic extraction. |
| Missing optional files are evidence limitations, not fatal errors. | `partial_valid` imports with warnings and unavailable evidence categories. | confirmed | Validation and future metrics | Future metrics must check evidence availability before computing. |
| Import idempotency is bundle fingerprint based. | Repeated import of the same bundle returns an idempotent result; conflicting run IDs are rejected. | confirmed | Registry integration | Preserve run-ID conflict checks in future import changes. |
| Phase 3 metrics consume canonical records only. | `metrics.engine.compute_metrics` takes `CanonicalTables`, run context, evidence availability, and config. | confirmed | Metrics engine | Keep metrics side-effect free and do not read raw files directly. |
| Linear interpolation is the Phase 3 percentile method. | `metrics.statistics.percentile_linear` is used by latency, utilisation, speed, trip, and aggregation P50/P95 metrics. | inferred | Metric formulas and golden tests | Revisit only if dissertation methodology requires a different percentile definition. |
| The default saturation threshold is synthetic-demo configuration, not validated traffic/VEC science. | `MetricEngineConfig.saturation_threshold` defaults to `0.90` and docs mark it configurable. | inferred | Infrastructure metrics, later R2 | Keep threshold overridable and avoid causal claims. |
| Rejected bundles should not produce numeric metrics. | Invalid-row fixture produces `invalid` metric results with `INVALID_SOURCE_DATA`. | confirmed | Metric availability | Fix source validation before computing metrics. |
| Evidence packs are the only diagnostic rules input. | `EvidencePack` embeds validation, evidence availability, metric config, metrics, and provenance; it has no narrative diagnosis fields. | confirmed | Phase 5 diagnostic rules | Keep rules decoupled from raw CSV and UI. |
| Single synthetic baseline/variation comparison is demonstrative only. | Comparison docs and models report deltas neutrally and aggregation reports replicate counts. | confirmed | Comparison and dissertation claims | Avoid algorithm conclusions from one run. |
| Registry metric storage can be JSON payloads in Phase 3. | Additive SQLite tables store metric collection and evidence-pack JSON; canonical row storage remains deferred. | inferred | Storage and aggregation | Move to richer storage only when query/data-volume evidence justifies it. |
| Phase 4 UI must remain thin. | UI service layer delegates validation, metrics, comparison, and evidence-pack building to Phase 1-3 modules. | confirmed | Streamlit pages | Keep formulas and validation logic out of page code. |
| Phase 4 data modes are synthetic, imported, and historical replay only. | UI labels and docs expose near-live and true-live only as unsupported future capabilities. | confirmed | Operations View and Home | Do not claim live Manchester data. |
| Direct launch remains unavailable in the UI. | Default capability manifest has `direct_launch=false`, and Scenario Studio renders a disabled Run button. | confirmed | Scenario Studio | Enable only after an adapter documents direct launch support. |
| Streamlit's runtime dependency tree includes pandas/pyarrow transitively. | Installing `streamlit` brought those packages into the environment. | confirmed | Dependency documentation | Do not import pandas/pyarrow directly in TrafficTwin Phase 4 code. |
| AppTest coverage does not fully reflect page rendering coverage. | Streamlit AppTest flow passes, but coverage reports low line coverage for page modules. | inferred | Coverage interpretation | Keep unit coverage focused on services, state, formatting, and chart data; use AppTest and smoke checks for page rendering. |
| Phase 5 rules consume EvidencePack only. | `rules.engine.evaluate_rules` accepts an `EvidencePack`; rule modules read metric values from the embedded metric collection. | confirmed | Diagnostic rules | Do not read raw CSV or canonical rows in rules. |
| R1 lacks direct T1-by-low-tier evidence in Phase 3 packs. | Existing metric keys provide task-class completion and vehicle-tier completion separately, not a cross-tab. | confirmed | R1 confidence | Trigger only from available T1, offload, and utilisation metrics; cap confidence and list missing low-tier context. |
| R2 lacks direct task-to-saturation temporal overlap in Phase 3 packs. | EvidencePack stores aggregate saturation and task-outcome metrics, not per-task/per-window overlap. | confirmed | R2 confidence | Use aggregate support for candidate hypotheses and list temporal-overlap evidence as missing context. |
| R3 requires experiment-level evidence not present in single-run bundles. | Single-run EvidencePacks do not include cross-algorithm dispersion or always-local gap metrics. | confirmed | R3 | Return `insufficient_evidence` for ordinary single-run packs. |
| Phase 5 thresholds are provisional synthetic-evaluation defaults. | `RuleSetConfig` stores thresholds and docs mark them non-validated. | inferred | R1-R3 | Keep thresholds configurable and avoid literature-backed or real-world claims. |
| Fault-injection precision/recall is an implementation check only. | Synthetic diagnostic cases are labelled in `tests/fixtures/diagnostics/cases.json`. | confirmed | Evaluation | Do not claim external diagnostic validity until real Randy/SUMO evidence and expert review exist. |
| Phase 6A found no real Randy/VEC artifacts. | This was true before Randy's GitLab package was cloned, but is now superseded by `external/tos-data`. | contradicted | Randy adapter, manifest builder, launcher | Use the updated integration docs; do not rely on the older no-artifact conclusion. |
| Phase 6A found Randy/VEC result artifacts in an external package. | `external/tos-data` contains evaluation CSV, training CSV/JSON, instrumented per-step NPZ, per-task NPZ, trace NPZ, and training records. | confirmed | Randy adapter, schema mapping, integration planning | Proceed only with discovery and mapping until ambiguous units/semantics and fixture permission are resolved. |
| Randy `task_type` maps to TrafficTwin task classes as `0 -> T1`, `1 -> T2`, `2 -> T3`. | In every inspected per-task NPZ showcase file, type-wise `task_met` rates exactly match JSON `t1_completion`, `t2_completion`, and `t3_completion` with this mapping. | inferred | Randy per-task converter | Use only with an adapter note or after Randy/source-code confirmation. |
| Randy `task_met` is deadline-met completion. | For inspected files, `task_met` exactly equals `task_lat_ms <= deadline` using T1/T3=100 ms and T2=500 ms, and aggregate `task_met` rate equals JSON `completion`. | inferred | Randy task conversion, completion metrics | Map to canonical `completed` only with explicit deadline-satisfied semantics. |
| Randy `rsu_load` is an RSU pressure/backlog signal. | In incident runs, `rsu_load` is tightly correlated with `rsu_busy_ms` and approaches `rsu_max_concurrent`; ordinary runs often have zero load despite V2I actions. | inferred | Infrastructure metrics, R2 | Do not call it queue length or utilisation until source semantics are confirmed. |
| Phase 6A found no raw SUMO XML/config artifacts. | The external package contains trace NPZ files but no `.sumocfg`, `.net.xml`, route, additional, FCD XML, tripinfo, detector, summary, or queue XML outputs. | confirmed | SUMO adapters and journey-time real integration | Keep SUMO XML adapter work blocked until actual formats are supplied. |
| Existing synthetic fixture schemas are not Randy schemas. | Synthetic manifests declare `source: synthetic_fixture` and `environment.name: synthetic`. | confirmed | Schema mapping | Use them only to document TrafficTwin's internal bundle contract; do not infer real columns or units. |
| Direct Randy/SUMO launch is unsupported in the current workspace. | `external/tos-data` is a result package; it does not include a complete CLI/API/script/notebook/job contract. | confirmed | Launcher and Scenario Studio Run control | Keep `direct_launch=false`; require documented and tested headless invocation before enabling. |
| Asynchronous Randy/SUMO launch is unsupported in the current workspace. | Training records mention CSF3/SLURM history, but no runnable job interface, scripts, or logs are present locally. | confirmed | Async execution | Keep `asynchronous_launch=false`; do not build a queue. |
| Real scenario-control support remains unknown. | Training records mention some flags/environment variables, but no safe config schema or invocation contract is present. | unknown | Capability manifest and Scenario Studio controls | Keep demand, workload, fleet, RSU, action-toggle, signal, and lane-closure controls `unknown` for Randy/SUMO. |
| Phase 6B requires ambiguity resolution before adapter coding. | `docs/integration/phase6_decision.md` records unresolved task-type, completion, RSU, trace-unit, fixture-permission, and execution-contract questions. | confirmed | Project planning | Ask Randy for precise semantics and approval before writing real adapters. |
| Generated documentation references should come from code. | `scripts/generate_reference_docs.py` generates Pydantic schemas, CLI help, metric catalogue, validation codes, and rule catalogue JSON. | confirmed | Documentation reproducibility | Regenerate references after public interfaces change. |
| Automated screenshot capture is not part of the repository. | No screenshot automation or stored screenshots existed during the documentation pass. | confirmed | Demo assets | Use `docs/assets/screenshots/README.md` as a manual capture checklist; do not create fake images. |
| The documentation pass must not change research functionality. | User explicitly scoped this phase to documentation and polish only. | confirmed | Current increment | Limit changes to docs and documentation-generation artifacts. |
| Provenance traces must be read-only. | The Provenance Explorer phase explicitly forbids calculation changes and source mutation. | confirmed | Provenance Explorer, CLI, UI | Build traces from existing artifacts and expose unavailable links when evidence is absent. |
| Aggregate metrics do not currently store full per-row contribution lists. | Phase 3 `MetricValue` stores metric value, evidence keys, warnings, and metadata, not materialised contributing row IDs. | confirmed | Metric provenance | Show required canonical tables, eligible record counts, bounded row samples, and limitations; do not invent row weights. |
| CSV source-row preview is sufficient for current bundles. | Current bundle contract and fixtures use CSV source files for canonical records. | confirmed | Source-row inspection | Support CSV previews now; future adapters can extend preview behavior when evidenced formats exist. |
| EvidencePack-only diagnostic fixtures cannot recover source rows. | Phase 5 fault-injection cases provide EvidencePacks, not run bundles. | confirmed | R1/R2 demo traces | Trace rule findings to metric evidence and mark canonical/source links unavailable. |
| Standalone demonstration must not depend on Randy or SUMO. | User explicitly requested a self-contained standalone product phase without real integration. | confirmed | Synthetic generator, demo workspace, reports, UI launch | Generate standard synthetic bundles and route them through the existing import-first pipeline. |
| Synthetic generator outputs are software fixtures, not calibrated simulations. | `SyntheticScenarioConfig` and docs describe simple deterministic relationships only. | confirmed | Demo interpretation, viva claims | Label all generated artifacts synthetic and avoid real-world validation claims. |
| Synthetic policy profiles are not real algorithms. | Profiles are named `synthetic-*` and documented as workload-generation behavior. | confirmed | Scenario labels, R3 demo | Do not claim MAPPO/DQN/Lyapunov behavior from standalone profiles. |
| Standalone report export should prefer bundle paths. | Report commands build from validated bundles and do not require registry-run lookup. | confirmed | Report CLI | Use bundle paths for reproducible reports; keep registry-run shortcuts as future work. |
| GitHub Actions is an appropriate CI example for this repository. | A `.github/workflows/ci.yml` file now runs local package quality gates and standalone smoke checks without secrets. | inferred | CI/release | If hosting changes, keep the workflow as a removable example and document equivalent commands. |
| Product Polish is presentation and workflow only. | New pages call existing generator, registry, reporting, validation, metrics, diagnostics, provenance, and search services. | confirmed | Streamlit UX | Do not add new scientific capability in UX polish work. |
| Scenario Builder is synthetic-only. | It validates `SyntheticScenarioConfig` and writes standard generated bundles. | confirmed | Scenario authoring | Keep Randy/SUMO controls unavailable until real adapters exist. |
| Report Dashboard regeneration is explicit. | The Reports page calls reporting builders only after a user action. | confirmed | Reporting workflow | Never regenerate reports automatically on page load. |
| Settings are session-scoped. | Settings are stored in Streamlit session state only. | confirmed | Local UX preferences | Do not imply authentication or persistent user profiles. |
