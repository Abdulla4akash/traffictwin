# TrafficTwin Assumption Register

Statuses:

- `confirmed`: directly evidenced in the current repository or user brief.
- `inferred`: reasonable working interpretation, but not directly proven.
- `unknown`: not evidenced.
- `contradicted`: existing evidence conflicts with another instruction or assumption.

| Assumption | Evidence | Status | Affected feature | Fallback behaviour |
|---|---|---|---|---|
| `diss/` is the TrafficTwin project root. | Abdulla explicitly confirmed `diss/` as the permanent TrafficTwin project root before Phase 1. | confirmed | File layout, docs, Phase 1 scaffold | Keep all new TrafficTwin files inside `diss/`. |
| The complete v0.4 design has been supplied locally. | The original `diss/AGENTS.md` contained `TrafficTwin - Design Proposal (v0.4)` and was read completely before relocation. | confirmed | Historical rationale and meeting traceability | Preserve `docs/traffictwin-design-v0_4.md` as the historical source. |
| The canonical v0.7 design path exists. | Abdulla approved Manchester historical/live integration, observed-to-SUMO and SUMO-to-VEC flow, interface redesign, and v0.6 release isolation in `docs/traffictwin-design-v0_7.md`. | confirmed | Agent discovery and future product/architecture decisions | Require agents to read v0.7 before architectural work and use implementation status for current truth; retain the immutable `v0.6.0` release as the implemented baseline. |
| Inclusion in the v0.5 design means the capability is implemented. | The v0.5 status model explicitly separates implemented, partial, planned, and blocked capabilities. | contradicted | Capability manifest, README, UI, dissertation claims | Do not change implementation claims until code, tests, docs, provenance, and acceptance evidence exist. |
| An analyst annotation can correct a metric, rule, provenance trace, or earlier annotation in place. | REP-02 stores annotations in a separate sequenced table; ADR-040 and SQLite triggers prohibit update/delete and report attachment leaves computed sections/claims unchanged. | contradicted | Reports, registry, dissertation review trail | Append a later correction or superseding decision; preserve all earlier history and never use commentary to mutate evidence. |
| A rendered report or section-body text diff proves that scientific evidence changed. | REP-03 and ADR-041 compare prose-free typed claim snapshots only; titles, timestamps, bodies, warnings, labels, commands, and analyst annotations are excluded from the scientific fingerprint. | contradicted | Report review, dissertation artifacts, regression interpretation | Regenerate structured report JSON, require compatible type/source/denominator/inventory, inspect typed JSON Pointer changes, and keep narrative-only sections unavailable. |
| A one-page executive summary may drop unavailable claims, warnings, or limitations to fit. | REP-04 and ADR-042 publish complete availability and omitted-highlight counts, retain every admitted warning and exact limitation, and fail PDF export when all content cannot fit on one A4 page. | contradicted | Supervisor review, dissertation summaries, report interpretation | Use the deterministic projection and its source/claim links; treat overflow as a visible refusal, not permission to hide caveats. |
| A registry search result with a higher score is a more important, severe, confident, or scientifically valid finding. | REP-05 and ADR-043 define category-neutral integer lexical relevance only, over path-redacted local text; a match does not validate or endorse the source record. | contradicted | Research discovery, report review, dissertation interpretation | Use search to locate the typed reference, then inspect its evidence, validation, provenance, and method-specific analysis. |
| Re-running additive `CREATE TABLE IF NOT EXISTS` statements is sufficient registry migration evidence. | OPS-01 and ADR-044 require an authoritative version, contiguous immutable checksummed ledger, per-version object/column validation, one atomic ordered plan, rollback, and historical preservation tests. | contradicted | Registry persistence, upgrades, caching/doctor prerequisites | Use the migration runner; inspect read-only status first, back up valuable registries, and fail closed on drift, unknown objects, future versions, or downgrades. |
| A successful registry schema migration proves that stored JSON is scientifically valid. | OPS-01 verifies SQLite structure and immutable migration history only; it deliberately does not parse, recalculate, or reinterpret stored scientific artifacts. | contradicted | Registry operations, dissertation claims | Use artifact-specific validation, metric/rule contracts, and provenance after structural migration. |
| A canonical cache hit may trust the bundle path or file modification times without rereading raw evidence. | OPS-02 and ADR-045 reopen and SHA-256 fingerprint every raw file, parse the current manifest, and derive the complete content key before probing an entry. | contradicted | Canonical caching, provenance, reproducibility | Keep raw evidence available; treat hashing as mandatory hit admission and use ordinary cold validation on a miss/bad entry. |
| Cached Parquet becomes primary evidence or a persistent canonical analytical database. | The cache contains checksummed derived tables outside the raw bundle; it is disposable, content-addressed, and not registered as raw evidence. | contradicted | Storage, dissertation claims, archival design | Preserve raw bundles unchanged and cite their fingerprint/source rows; rebuild or discard derived cache independently. |
| TrafficTwin may silently delete or overwrite a stale, incompatible, or corrupt cache entry. | OPS-02 classifies and bypasses bad entries, preserves them for operator inspection, and writes only when the exact key path is absent. | contradicted | Cache recovery, shared workspaces | Report the exact state/path, run cold validation if needed, and require an explicit operator decision before quarantining/removing derived data. |
| Running `traffictwin doctor` may initialise/migrate a registry or warm/repair a cache to test it. | OPS-03 and ADR-046 use only the OPS-01 immutable SQLite inspector and OPS-02 read-only probe; v1 has no repair path. | contradicted | Operations, registry, cache, raw evidence | Report the observed state and explicit remediation only; require a separate intentional command for any mutation. |
| A missing optional command or deliberately unsupported launcher means the core installation is unhealthy. | OPS-03 derives overall state only from checks required by the selected invocation and keeps optional/unsupported capability truth separately visible. | contradicted | Installation diagnosis, SUMO/TOS integration | Keep the generic import-first runtime healthy when its required checks pass; label the optional capability unavailable or blocked. |
| A successful doctor report proves external permission, scientific validity, or future filesystem access. | ADR-046 limits doctor to point-in-time operational metadata, structural integrity, immutable probes, and advisory `os.access`; unknown permissions remain unavailable. | contradicted | Dissertation claims, operations, public sharing | Use source-specific validation, scientific evaluation, written permission, and workflow-time access checks separately. |
| A checksummed RO-Crate proves publication permission, licence validity, authorship, or scientific truth. | OPS-04 and ADR-047 verify declared policy, byte identity, typed structure, and lineage only; all legal/publication statements remain caller-owned. | contradicted | Research archiving, citation, public release | Review permissions/licences independently, retain the declaration basis, and interpret checksums as identity rather than validity. |
| Reference mode is safe for public unknown external evidence because it omits raw bytes. | Relative names, sizes, and hashes can still disclose sensitive structure or enable correlation; ADR-047 requires confirmed permission for a public imported reference. | contradicted | RO-Crate, private data, publication | Use `exclude` until permission and a raw licence are confirmed; private reference archives still require controlled handling. |
| TrafficTwin may infer a DOI, project licence, publication date, or raw-evidence reuse right during archive creation. | OPS-04 requires caller declarations, the repository has no selected licence, and archive determinism requires an explicit publication date. | contradicted | Citation, archive metadata, release | Keep the no-additional-rights default visible; supply only verified human-owned metadata. |
| Two sources implementing `ExternalSourceAdapter` have equivalent fields, evidence, or scientific outputs. | OPS-05 standardises disclosure of each source boundary, not the underlying semantics; SUMO is partial canonical trips while TOS is aggregate/source-specific. | contradicted | External adapters, cross-source comparison | Check exact field, unit, population, conversion, provenance, and downstream metric compatibility contracts. |
| A discovery-marker match means that an external package is valid and importable. | Discovery is intentionally shallow; only the selected source validator can return its declared import outcome. | contradicted | External-source discovery | Treat `one_match` as adapter selection evidence only, then inspect and validate the unchanged source. |
| `partial_canonical` is a better or more trustworthy source than `aggregate_summary`. | OPS-05 conversion profiles are non-ordinal descriptions of produced artifacts and say nothing about realism, scientific validity, or evidence quality. | contradicted | External-source interpretation | Read the exact output and blocker lists; never rank sources by conversion label. |
| Operational vehicle tier is a protected or demographic fairness attribute. | The canonical field describes only a supplied compute/resource tier; no protected, demographic, socioeconomic, or personal attributes exist in the contract. `ADR-019` fixes the interpretation boundary. | contradicted | `MET-04`, UI, reports, dissertation claims | Label outputs operational group-balance evidence only; never infer protected attributes. Require exact stable group membership, two groups, support of two per group, and complete coverage. |
| Missing or thin operational groups can be silently excluded from fairness calculations. | `OperationalFairnessPolicy` v1.0 and `ADR-019` require complete coverage and minimum support for every observed group. | contradicted | Vehicle-tier/RSU grouped metrics and comparisons | Return explicit group-coverage/support unavailability and preserve the policy/group-set fingerprints. |
| A canonical V2I `target_id` always proves an execution-target RSU. | A column name and decision code do not establish external target semantics; `ADR-020` requires an explicit `TaskRsuTargetContract` plus complete exact joins. | contradicted | `MET-05` per-RSU task outcomes | Keep the family unavailable without the contract; never infer or assign missing targets. |
| Vehicle `x/y` values are automatically geographic coordinates. | Existing replay preserves source coordinates without a CRS. `ADR-020` requires an explicit named metre-based source frame and fixed grid contract for spatial summaries. | contradicted | `MET-05`, replay, maps, dissertation claims | Label cells source-frame/non-geographic; do not infer latitude/longitude, Manchester locations, nearest RSUs, or task positions. |
| A custom metric API must accept uploaded Python or discover ambient packages automatically. | `ADR-021` fixes an explicit trusted in-process registry; automatic or uploaded code would make the active scientific catalogue unsafe and environment-dependent. | contradicted | `MET-06`, CLI/UI, reproducibility | Require reviewed local registration in application code; provide no module-path loader, upload control, `eval`, or implicit entry-point discovery. |
| Running a custom metric twice proves that its implementation is globally deterministic and scientifically valid. | Equal canonical outputs test repeatability only for the evaluated input; trusted Python is not sandboxed and may still contain hidden state or invalid methodology. | contradicted | `MET-06`, dissertation claims | Label the check evaluated-input repeatability, retain author/tests/version responsibilities, and never claim proof of purity or scientific validity. |
| A declarative rule file can safely contain arbitrary YAML expressions because `SafeLoader` is used. | `ADR-023` requires a separate closed typed grammar, byte/predicate bounds, duplicate/alias/tag rejection, and no imports, functions, formulas, templates, dynamic keys, or nested rule references. | contradicted | `DIA-04`, local extension security | Admit only trusted local static definitions through the published parser and retain the definition fingerprint; do not call the grammar a sandbox. |
| The default R7 gap is an externally validated fairness standard. | No protected attributes, external calibration, significance method, or literature-backed threshold are present. The selected dimensions are operational vehicle tier or observed exact execution target only. | contradicted | `DIA-02`, reports, dissertation claims | Label `0.20` provisional, predeclare study thresholds, require exact complete supported groups, and avoid discrimination, geography, significance, or causal claims. |
| A public SUMO output adapter also authorises direct SUMO execution. | v0.5 admits immutable completed SUMO outputs only; no launcher contract is evidenced. | contradicted | SUMO adapter, UI controls, CLI | Keep direct launch unsupported and separate from output ingestion. |
| Phase 1 TrafficTwin implementation exists. | `src/traffictwin/`, `pyproject.toml`, examples, and tests were added in Phase 1. | confirmed | Phase 2 foundation | Build Phase 2 on the tested package scaffold. |
| No real data is available in the workspace. | Superseded by Randy's external `TOS Data` package cloned under `external/tos-data` on 2026-07-18. | contradicted | Adapters, metrics, validation | Continue using synthetic fixtures for standalone demos, but use the external package for Phase 6B discovery. |
| Randy's runnable environment is available locally. | VEC-07 safely stages exact audited blobs under a controlled namespace-only import path, VEC-08 completed two full 32,400-step JAX/JAXLIB 0.4.30 CPU runs, and VEC-10 exposes only request-preflight-gated foreground execution. | confirmed with scope | Runner, reproduction verifier, VEC workbench | Preserve import-first behavior; do not infer SUMO launch, training, background scheduling, or other-platform support. |
| Randy's occupancy tables provide exact persistent vehicle identity for the five prepared traces. | `VEC-01` reconstructed every trace mask from inclusive occupancy spans with zero slot/vehicle overlaps and exact active-second equality. Identity is exact only within each admitted visit span; recycled slots retain slot-level tier/EV attributes. | confirmed | `VEC-02`–`VEC-05`, replay, grouped metrics | Use `sumo_vehicle_id` only inside validated occupancy intervals and never treat a slot as one persistent vehicle. |
| Randy's `veh_action` value proves that an offload transfer occurred. | Across 60 per-step files, 4,368 V2I and 55,658 V2V decision rows with tasks selected the action while the corresponding best-target field was `-1`; evaluator code gates target use on eligibility. | contradicted | `VEC-04`, action/target metrics, dissertation wording | Describe actions as policy selections and require a non-negative eligible target before making a target/offload claim. |
| Randy's per-task rows expose eventual physical completion and per-task energy. | All six pinned per-task NPZ files contain only `task_type`, `task_lat_ms`, `task_met`, and `task_active`; completion summaries count deadline success and energy exists only as a run aggregate. | contradicted | `VEC-02`, `VEC-09`, energy/completion metrics | Keep eventual completion and per-task energy unavailable; do not rename deadline success or distribute aggregate energy across tasks. |
| Every occupancy vehicle has a completed tripinfo row. | Exact-ID coverage is complete only for `wd_am`; the other four traces have quantified unmatched vehicles, mostly ending at the trace boundary, plus 14 earlier incident exits with no assigned cause. | contradicted | `VEC-05`, journey-time metrics | Publish matched eligibility/exclusions, preserve the full-day clock, and compute journey metrics only on explicitly matched vehicles. |
| Direct launch is uniformly supported. | VEC-10 can expose one exact allowlisted evaluator only after request-specific VEC-07 preflight; generic CSV and SUMO adapters have no launcher. | contradicted | Scenario Studio, VEC workbench, capability manifests | Keep generic/SUMO controls disabled and label VEC foreground execution conditional, never global. |
| Randy's evaluation-summary CSV schema is known. | `external/tos-data/evals/eval_results_master.csv` and `DATA_DICTIONARY.md` document a 300-row evaluation summary table. | confirmed | Integration, metric reconciliation | Import only as explicitly source-provided summaries with a distinct metric version; do not call them canonical rows. |
| Public SUMO trip and summary records are available for adapter acceptance. | `tests/fixtures/sumo/square_public` contains checksummed SUMO 1.27.1 outputs generated from the official licensed square scenario. | confirmed | `ING-01`, Journey-Time Lens, SUMO UI | Validate the declared source contract; keep fixture results labelled synthetic simulation evidence. |
| SUMO summary `running` is equivalent to canonical traffic interval `count`. | Official SUMO semantics define `running` as vehicles present in the time step, while current TrafficTwin count metrics sum interval observations. | contradicted | SUMO summary mapping, traffic metrics | Retain `SumoSummaryStep` source evidence; do not map occupancy to canonical traffic count. |
| Per-RSU queue and utilisation are exported over time. | Design notes queue state may exist but export is unconfirmed. | unknown | Infrastructure metrics, R2 | Compute infrastructure metrics only from valid `infra_state.csv`; otherwise return unavailable. |
| A declared `J` column alone makes task-energy metrics semantically valid. | `ADR-018` shows that quantity, row level, eligibility, and delay unit are also required; generated synthetic bundles can declare the strict v1.0 contract, while current SUMO/TOS sources cannot. | contradicted | `MET-03`, comparisons, external adapters | Require `TaskEnergyContract`, exclude missing values rather than using zero, reject negatives, and require matching contract fingerprints across runs. |
| Task decisions include local, V2I, and V2V. | User brief defines these allowed decisions; Randy's data dictionary maps `veh_action` values 0, 1, 2 to local, V2I, and V2V in per-step arrays. | confirmed | Seed schema, validation, metrics, Randy adapter | Use these labels in synthetic and generic contracts; adapter can map Randy integer labels after scope approval. |
| Unknown fields should remain absent or null. | User brief explicitly requires absent fields not be fabricated. | confirmed | Canonical models, validation, metrics | Preserve nulls; mark dependent metrics unavailable. |
| Deterministic code must compute all metrics. | User brief states numbers come from deterministic code. | confirmed | Metrics engine, rules, UI | Keep LLM out of metric and diagnosis computation. |
| Diagnostic outputs are hypotheses, not proven causes. | User brief and design both require softened causal claims. | confirmed | Rules engine and UI copy | Use `hypothesis` language and include alternatives/missing evidence. |
| Earlier XITS instruction says not to lock design before `MATERIALS COMPLETE`. | `XITS/README.md` and `XITS/research_context.md` contain that gate, but Abdulla explicitly superseded it for implementation purposes, first through v0.4 and now through the approved TrafficTwin v0.5 specification. | contradicted | Planning authority | Preserve XITS as historical research material, not an active implementation blocker. |
| The first vertical slice should be synthetic. | No real data or simulator exists locally; user permits synthetic fixtures if no real data exists. | confirmed | Phase 1-4 planning | Build synthetic baseline/variation demonstration and label it synthetic. |
| SQLite is sufficient for Phase 1 registries. | Phase 1 registry uses standard library `sqlite3`; tests verify persistence across registry instances. | confirmed | Metadata registry | Keep SQLite for metadata; defer DuckDB/Parquet until run volume justifies it. |
| Streamlit is the first UI. | User explicitly lists Streamlit as preferred. | confirmed | Phase 4 UI | Keep UI thin over tested library services. |
| FastAPI, React, queues, and cloud infrastructure are out of scope. | User explicitly excludes them unless demonstrated requirement exists. | confirmed | Architecture | Do not add these in the protected vertical slice. |
| The package should target Python 3.11+. | User explicitly specifies Python 3.11+. | confirmed | Packaging and typing | Configure package metadata for Python 3.11 or newer. |
| The local quality-gate interpreter is Python 3.12. | `/opt/homebrew/bin/python3.12` is available; system `python3` is 3.9.6. | confirmed | Tooling, tests | Use Python 3.12 for local validation and declare Python 3.11+ support in packaging. |
| PyYAML is sufficient for Phase 1 seed files. | Phase 1 deterministic seed load/dump tests pass with PyYAML. | confirmed | Seed I/O | Reconsider ruamel.yaml only if comment-preserving edits become a requirement. |
| No unsupported Randy capability defaults to `true`. | Phase 1 tests and smoke assertion check all Randy-environment capability fields. | confirmed | Capability-aware UI and launch controls | Keep direct launch `false` and unconfirmed controls `unknown` until evidence changes. |
| Phase 1 registry stores metadata only. | `storage.registry` stores seed, experiment, and run JSON payloads; no canonical traffic/task tables exist. | confirmed | Storage boundary | Add canonical data storage only after Phase 2 ingestion and validation design is stable. |
| Phase 2 canonical records are in-memory only. | `CanonicalTables` holds lists of Pydantic records; SQLite stores run and bundle metadata, not row data. | confirmed | Storage boundary | Keep row storage deferred until metrics/data-volume evidence justifies it. |
| Generic tabular import is manifest-driven. | `GenericTabularAdapter` uses explicit format/compression declarations, column maps, and units for CSV, gzip-CSV, and Parquet. | confirmed | Adapter boundary | Do not add Randy-specific assumptions or suffix-based format inference to the generic adapter. |
| Equivalent compression should collapse raw bundle identity. | `ADR-013` retains the exact relative-path/raw-SHA-256 fingerprint so immutable evidence changes remain visible; canonical and metric projections establish semantic equivalence separately. | contradicted | Bundle idempotency and provenance | Recompression/conversion creates a distinct raw fingerprint and registry identity. |
| Header or numeric similarity is sufficient to authoritatively map an external CSV. | `ING-02` produces a non-executable draft, limits value patterns to distinctive task vocabularies, and requires explicit file/field/unit confirmation. | contradicted | Manifest inference, generic CSV adapter | Keep ties unresolved, require units unless evidenced by a supported header suffix, and route only confirmed manifests into normal validation. |
| Directory and ZIP bundles should be equivalent. | Tests compare directory and ZIP validation results. | confirmed | Bundle loading | Preserve ZIP safety checks and deterministic extraction. |
| Missing optional files are evidence limitations, not fatal errors. | `partial_valid` imports with warnings and unavailable evidence categories. | confirmed | Validation and future metrics | Future metrics must check evidence availability before computing. |
| Import idempotency is bundle fingerprint based. | Repeated import of the same bundle returns an idempotent result; conflicting run IDs are rejected. | confirmed | Registry integration | Preserve run-ID conflict checks in future import changes. |
| A batch import should be one all-or-nothing transaction. | `ING-04` requires failure isolation, while the ordinary registry operation is already transaction-scoped per bundle. | contradicted | Batch bundle import | Resolve/limit the set before mutation, then retain one transaction and unchanged idempotency/conflict rules per accepted candidate. |
| Batch import may discover bundles outside caller-supplied paths/globs. | `ADR-014` defines an explicit-input contract with deterministic expansion, deduplication, and ordering. | contradicted | Batch bundle import | Never scan implicitly; report unmatched globs and enforce 64-reference/256-candidate preflight limits. |
| Raising the ordinary decoded-table limit provides streaming ingestion. | Ordinary validation still materialises complete rows and canonical tables; `ING-05` instead uses bounded iterators and disk-backed global checks. | contradicted | Streaming canonicalisation | Keep the ordinary 10,000,000-byte limit and require explicit streaming mode for larger tables. |
| Chunk boundaries may change canonical or validation output. | Property/golden tests compare several chunk sizes across CSV, gzip-CSV, and Parquet, including global duplicates/references. | contradicted | Streaming canonicalisation | Treat chunking only as execution strategy; preserve source order and exact global reconciliation. |
| Streaming bounds cover the caller's consumer, report growth, and all native process memory. | Chunk estimates are format-specific parser controls; consumer retention is caller-owned and `tracemalloc` excludes some native allocations. | contradicted | Streaming performance claims | Publish the exact measurement method and use summary-only/non-retaining consumers for bounded operation. |
| Adjacent time windows may both include a record on their shared boundary. | `ADR-016` and boundary tests use half-open `[start,end)` intervals. | contradicted | Windowed metrics | Assign an exact end-boundary record only to the later aligned window. |
| Every task and trip outcome can be assigned by completion/arrival time. | Task completion and trip arrival timestamps are optional, while every accepted task has arrival and every trip has departure; cohort denominators must retain incomplete records. | contradicted | Windowed task/trip metrics | Anchor tasks by `arrival_time_s` and trips by `departure_time_s`; label the results as cohort outcomes. |
| A window's coverage fraction measures sensor or sampling completeness. | The canonical contract has no common sampling interval or sensor-uptime field. | contradicted | Temporal charts and exports | Define coverage only as requested-range overlap divided by grid width and state the limitation with every series. |
| Empty windows should be omitted or represented by numeric zero. | No admitted canonical row is an evidence-availability state and does not prove zero real activity. | contradicted | Windowed availability | Keep the window visible and reuse ordinary `unavailable` metric reason codes. |
| Window metrics can always be added or averaged to reproduce the whole run. | Ratios, means, percentiles, distinct/grouped values, Jain indices, traffic coverage, and boundary-clipped saturation episodes are non-additive. | contradicted | Temporal interpretation | Use each slice directly; claim partition addition only for a definition where cohort/count semantics support it. |
| Phase 3 metrics consume canonical records only. | `metrics.engine.compute_metrics` takes `CanonicalTables`, run context, evidence availability, and config. | confirmed | Metrics engine | Keep metrics side-effect free and do not read raw files directly. |
| Linear interpolation is the versioned percentile method. | `ADR-017`, `linear-rank-n-minus-1-v1` metadata, and golden tests establish sorted rank-`n-1` interpolation for task-latency P50/P95/P99; the helper is also used by other documented percentile metrics. | confirmed | Metric formulas and golden tests | A different estimator requires an explicit versioned contract change. |
| P99 requires at least 100 observations or represents a worst-case/confidence bound. | The implemented sample percentile is mathematically defined from one valid value under the default policy, but small samples provide weak tail evidence and no probabilistic guarantee. | contradicted | Task-latency interpretation | Carry sample count/method metadata, warn on singletons, allow a configured minimum, and describe P99 only as a sample percentile. |
| The default saturation threshold is synthetic-demo configuration, not validated traffic/VEC science. | `MetricEngineConfig.saturation_threshold` defaults to `0.90` and docs mark it configurable. | inferred | Infrastructure metrics, later R2 | Keep threshold overridable and avoid causal claims. |
| Rejected bundles should not produce numeric metrics. | Invalid-row fixture produces `invalid` metric results with `INVALID_SOURCE_DATA`. | confirmed | Metric availability | Fix source validation before computing metrics. |
| Evidence packs are the only diagnostic rules input. | `EvidencePack` embeds validation, evidence availability, metric config, metrics, and provenance; it has no narrative diagnosis fields. | confirmed | Phase 5 diagnostic rules | Keep rules decoupled from raw CSV and UI. |
| Single synthetic baseline/variation comparison is demonstrative only. | Comparison docs and models report deltas neutrally and aggregation reports replicate counts. | confirmed | Comparison and dissertation claims | Avoid algorithm conclusions from one run. |
| Registry metric storage can be JSON payloads in Phase 3. | Additive SQLite tables store metric collection and evidence-pack JSON; canonical row storage remains deferred. | inferred | Storage and aggregation | Move to richer storage only when query/data-volume evidence justifies it. |
| Phase 4 UI must remain thin. | UI service layer delegates validation, metrics, comparison, and evidence-pack building to Phase 1-3 modules. | confirmed | Streamlit pages | Keep formulas and validation logic out of page code. |
| Phase 4 data modes are synthetic, imported, and historical replay only. | UI labels and docs expose near-live and true-live only as unsupported future capabilities. | confirmed | Operations View and Home | Do not claim live Manchester data. |
| Direct launch remains unavailable in the UI. | Default capability manifest has `direct_launch=false`, and Scenario Studio renders a disabled Run button. | confirmed | Scenario Studio | Enable only after an adapter documents direct launch support. |
| Streamlit's transitive PyArrow dependency is sufficient for a core Parquet contract. | `ING-03` imports PyArrow in library ingestion, independently of Streamlit UI behavior. | contradicted | Dependency documentation | Declare `pyarrow>=18` directly in the core project dependencies. |
| AppTest coverage does not fully reflect page rendering coverage. | Streamlit AppTest flow passes, but coverage reports low line coverage for page modules. | inferred | Coverage interpretation | Keep unit coverage focused on services, state, formatting, and chart data; use AppTest and smoke checks for page rendering. |
| Phase 5 rules consume EvidencePack only. | `rules.engine.evaluate_rules` accepts an `EvidencePack`; rule modules read metric values from the embedded metric collection. | confirmed | Diagnostic rules | Do not read raw CSV or canonical rows in rules. |
| R1 lacks direct T1-by-low-tier evidence in Phase 3 packs. | Existing metric keys provide task-class completion and vehicle-tier completion separately, not a cross-tab. | confirmed | R1 confidence | Trigger only from available T1, offload, and utilisation metrics; cap confidence and list missing low-tier context. |
| R2 lacks direct task-to-saturation temporal overlap in Phase 3 packs. | EvidencePack stores aggregate saturation and task-outcome metrics, not per-task/per-window overlap. | confirmed | R2 confidence | Use aggregate support for candidate hypotheses and list temporal-overlap evidence as missing context. |
| R3 requires experiment-level evidence not present in single-run bundles. | Single-run EvidencePacks do not include cross-algorithm dispersion or always-local gap metrics. | confirmed | R3 | Return `insufficient_evidence` for ordinary single-run packs. |
| Diagnostic thresholds are provisional synthetic-evaluation defaults. | `RuleSetConfig` stores thresholds and docs mark them non-validated. | inferred | R1-R5 | Keep thresholds configurable and avoid literature-backed or real-world claims. |
| Fault-injection precision/recall is an implementation check only. | Synthetic diagnostic cases are labelled in `tests/fixtures/diagnostics/cases.json`. | confirmed | Evaluation | Do not claim external diagnostic validity until real Randy/SUMO evidence and expert review exist. |
| Phase 6A found no real Randy/VEC artifacts. | This was true before Randy's GitLab package was cloned, but is now superseded by `external/tos-data`. | contradicted | Randy adapter, manifest builder, launcher | Use the updated integration docs; do not rely on the older no-artifact conclusion. |
| Phase 6A found Randy/VEC result artifacts in an external package. | `external/tos-data` contains evaluation CSV, training CSV/JSON, instrumented per-step NPZ, per-task NPZ, trace NPZ, and training records. | confirmed | TOS read-only integration, schema mapping | Use the implemented source-summary/replay boundary; keep canonical conversion and launch gated. |
| Randy `task_type` maps to TrafficTwin task classes as `0 -> T1`, `1 -> T2`, `2 -> T3`. | `vec_env/jaxmarl/env/vec_jax.py` defines these codes; all supplied per-task showcases also reconcile. | confirmed | TOS task inspection | Use for bounded source inspection; preserve integer-code provenance. |
| Randy `task_met` is deadline success. | Environment source defines the outcome by latency versus class deadline; all supplied active entries reconcile. | confirmed | TOS task inspection, source summary | Label it `deadline_met`; do not map it to eventual canonical completion. |
| Randy `rsu_load` is in-flight task count and `rsu_busy_ms` is remaining compute backlog. | `vec_jax.py` updates these states directly; `rsu_max_concurrent` is the in-flight concurrency ceiling. | confirmed | RSU replay inspection, infrastructure availability, R2 | Expose active tasks/backlog and concurrency pressure; do not call them queue length or CPU utilisation. |
| A public static demonstration may use repository-generated synthetic fixtures only. | Standalone fixtures carry explicit synthetic provenance and use no external package. | confirmed | Netlify deployment | Stage only precomputed synthetic metrics, diagnostics, and reports. |
| Permission to use Randy-derived material is unrestricted. | Randy's 21 July 2026 response permits sanitised samples and aggregates in the repository/dissertation with citations, engine `v2_post_nrsus_fix`, and `_s102` selected-seed labelling; it does not grant blanket redistribution of full raw data, repositories, checkpoints, or third-party assets. | contradicted | Public TOS atlas, fixtures, screenshots, dissertation | Admit only a permission-manifested sanitised/aggregate subset; keep all broader reuse unavailable until separately licensed or authorised. |
| Pseudonymised VEC-11 sample rows are anonymous. | VEC-11 removes direct IDs, mappings, clocks, slots, targets, and paths and rounds numeric values, but the tiny linked sample may remain indirectly distinguishable. | contradicted | Dissertation sample, privacy, publication | State that pseudonymisation is not anonymity; publish only the three permission-manifested rows and do not retain or publish an identity mapping. |
| A project licence has been selected. | No licence file or explicit selection exists. | unknown | Public source release | Keep `Licence not yet specified` visible and do not invent a licence. |
| Processed trace time, position, and speed units are seconds, metres, and metres per second. | `build_trace.py` copies SUMO FCD timestep/x/y/speed; source documentation fixes SUMO 1.27.0 and standard FCD units. | confirmed | Historical replay | Label units explicitly; keep time-local slot identity limitations visible. |
| Phase 6A found no raw SUMO XML/config artifacts in Randy's external package. | The audited update adds four gzip tripinfo XML outputs, but still provides no source FCD/network pair, `.sumocfg`, routes, detectors, or queues. | contradicted with scope | TOS/VEC canonical conversion | Use only the exact audited tripinfo joins; do not infer absent SUMO inputs/configuration or treat the separate public SUMO fixture as Randy-run evidence. |
| Existing synthetic fixture schemas are not Randy schemas. | Synthetic manifests declare `source: synthetic_fixture` and `environment.name: synthetic`. | confirmed | Schema mapping | Use them only to document TrafficTwin's internal bundle contract; do not infer real columns or units. |
| Direct Randy/SUMO launch is uniformly supported in the current workspace. | VEC-07/VEC-08 verify only one exact evaluator path, and VEC-10 exposes it conditionally after request-specific preflight; generic and SUMO launch still lack an accepted adapter. | contradicted | Launcher and Scenario Studio Run control | Keep generic/SUMO `direct_launch=false`; expose only the pinned VEC foreground path and never imply global launch support. |
| Asynchronous Randy/SUMO launch is unsupported in the current workspace. | A CSF SLURM wrapper exists but depends on external paths/environment and is not an observable local job interface. | confirmed | Async execution | Keep `asynchronous_launch=false`; do not build a queue. |
| Source scenario controls exist. | Source exposes task arrival/mix/ordering, fleet, vehicle count, RSU placement/count, and capacity mechanisms; action toggles, signal timing, and lane closure are not evidenced as external scenario controls. | confirmed | Source contract and capability manifest | Document source controls, but keep every control disabled in the read-only TrafficTwin adapter. |
| Full canonical TOS conversion is now justified by accepted identity and trip joins. | VEC-03/VEC-05 resolve scoped identity/trips, but eventual physical completion, per-task energy, confirmed transfer/execution targets, and compatible canonical queue/utilisation evidence remain absent. | contradicted | Canonical converter, infrastructure metrics | Admit only contract-compatible projections and keep the remaining source-state views and dependent canonical metrics unavailable. |
| The pinned source can reproduce the supplied instrumented NPZ outputs within an accepted numerical tolerance. | VEC-08 ran the full pinned weekend protocol-seed case twice. Both local runs were exact for all scientific arrays; against pinned source evidence, 61 checks were exact, average energy and per-step latency reductions passed the frozen float32 bounds, and no check mismatched. | confirmed with scope | VEC-08 CPU reproduction | Apply this tolerance only to the recorded JAX/JAXLIB 0.4.30 CPU case; do not generalise across hardware, scenarios, policies, or missing expected per-task values. |
| The audited evaluator can execute locally without editing either external repository. | VEC-07 stages exact evaluator/environment/actor blobs under a controlled namespace-only `PYTHONPATH`; VEC-08 repeated the full weekend run, and VEC-10 delegates its conditional foreground control to that same service. | confirmed with scope | VEC-07–VEC-10 local execution | Do not infer SUMO launch, training, persistent scheduling, or cross-platform support. |
| A UI run button may bypass the typed evaluator request or preflight for convenience. | VEC-10 loads strict VEC-06/VEC-07 request JSON and disables execution until the matching complete preflight is accepted. | contradicted | VEC-10 CLI/UI | Keep request construction and argv generation in tested library services; expose no command or extra-flags field. |
| A checksummed VEC-12 archive proves scientific truth or expands publication rights. | VEC-12 proves deterministic byte identity and binds accepted evidence/limitations; it carries no raw evidence and explicitly retains unavailable science, permission, licensing, and hosting limits. | contradicted | Dissertation reproducibility, publication, review | Use the archive for offline lineage/integrity verification only; retain every limitation and seek separate authority/evidence for stronger claims. |
| A complete Manchester-to-VEC lineage proves that a Randy policy is valid for Manchester. | MAN-11 records only a contiguous fingerprint-bound software/evidence chain. Calibration remains a model, VEC analysis sites are not canonical infrastructure, and no transfer, causal, or domain-validity design is introduced by provenance. | contradicted | Manchester case study, VEC interpretation, dissertation claims | Use the lineage to reproduce and audit artifact flow; evaluate domain validity separately under a predeclared scientific design and retain the fixed negative claims. |
| An FCD XML cryptographically identifies its matching network file. | VEC-06 can validate exact hashes, projection metadata, coordinate envelope, and one-second structure, but the FCD format carries no authenticated network-file identity. | unknown | VEC-06 input pairing and provenance | Require the caller to declare the pair; record both hashes and preserve this limitation in every receipt. |
| The pinned FCD builder safely accepts non-one-second input. | `build_trace.py` records `dt=1.0` rather than deriving it; VEC-06 therefore rejects any timestep sequence that is not finite, strictly ordered, and exactly one second apart before execution. | contradicted | VEC-06 trace correctness | Keep the wrapper admission check mandatory and do not call the source builder directly from TrafficTwin workflows. |
| Greedy FCD coverage sites represent real deployed RSUs. | VEC-06 produces deterministic grid-cell centres that cover observed points; neither input supplies deployment identity for those generated sites. | contradicted | VEC-06 placement interpretation, VEC-07 runner | Label them analysis sites and require separate evidenced infrastructure data for a real-RSU claim. |
| TOS evaluation summaries may be represented as MetricCollections only when labelled as source-provided. | `integration.tos.metrics` uses a separate version, warnings, source row metadata, and explicit unavailable results. | confirmed | Comparison, aggregation, evidence, provenance | Never describe these values as TrafficTwin recomputation from canonical rows. |
| TOS padded vehicle slots are persistent vehicle IDs. | Complete trace inspection found slot disappearance/reuse and discontinuities inconsistent with stable identity. | contradicted | Replay, vehicle canonicalisation | Use time-local references such as `slot:n@time-index:t`; do not join slots as one vehicle. |
| A reconciled occupancy slot identifies the same vehicle outside its recorded interval. | VEC-03 reconstructs all five masks exactly from inclusive spans and observes slot reuse; identity coverage is exact only inside each span. | contradicted | VEC-03 mobility and future task/trip joins | Bind downstream joins to the accepted trace fingerprint and resolve `sumo_vehicle_id` only when `t_enter <= time_index <= t_exit`. |
| A selected V2I/V2V action proves a transfer or `-1` target proves failure. | VEC-04 found 1,907 task rows with a selected offload action and no eligible target; the writer records decision-time eligibility, not transfer outcome. | contradicted | VEC-04 task evidence and later metrics | Preserve action and target availability separately; keep transfer confirmation, failure cause, and link quality unavailable. |
| Tripinfo full-day seconds may be directly aligned to trace-relative occupancy indices. | VEC-05 joins exact vehicle IDs while preserving both clocks; no evidence establishes a direct index equality. | contradicted | VEC-05 journeys | Join only on exact ID, retain original trip times, and report censoring/exclusions separately. |
| VEC deadline success may be reported as canonical physical task completion. | VEC-09 admits `task_met` only through separately named `tos.task.deadline_success.*` metrics; the source has no eventual physical-completion field. | contradicted | VEC-09 metrics, R1, R7 | Keep canonical completion metrics unavailable and never substitute deadline success. |
| Diagnostic thresholds may be selected from the same `_s102` evidence used to report VEC metrics. | VEC-09 performs admission only and records no predeclared windows, threshold, or development/held-out split. | contradicted | VEC-09 rule readiness, R6 | Design and register a separate calibration/validation protocol before evaluating a threshold or emitting a finding. |
| Generated documentation references should come from code. | `scripts/generate_reference_docs.py` generates Pydantic schemas, CLI help, metric catalogue, validation codes, and rule catalogue JSON. | confirmed | Documentation reproducibility | Regenerate references after public interfaces change. |
| Automated screenshot capture is a complete accessibility evaluation. | `scripts/ui_browser_audit.py` captures desktop/mobile pages and checks a bounded semantic rule set only. | contradicted | UI verification | Treat it as regression evidence; retain manual keyboard, screen-reader, contrast, and participant testing. |
| The documentation pass must not change research functionality. | User explicitly scoped this phase to documentation and polish only. | confirmed | Current increment | Limit changes to docs and documentation-generation artifacts. |
| Provenance traces must be read-only. | The Provenance Explorer phase explicitly forbids calculation changes and source mutation. | confirmed | Provenance Explorer, CLI, UI | Build traces from existing artifacts and expose unavailable links when evidence is absent. |
| Aggregate `MetricValue` objects materialise full per-row contribution lists. | Phase 3 values do not store row IDs, but `MetricContributionReport` reconstructs every accepted candidate row and its eligibility state on demand from the validated bundle. | contradicted | Metric provenance | Export the complete accepted-row ledger when requested; never invent causal weights or silently include pre-canonicalisation rejects. |
| Source-row preview must remain CSV-only. | `ING-03` now admits declared gzip-CSV and flat scalar Parquet through the generic contract, and provenance uses the same bounded decoder. | contradicted | Source-row inspection | Preview all declared generic tabular formats and keep the record locator stable across equivalent representations. |
| EvidencePack-only diagnostic fixtures cannot recover source rows. | Phase 5 fault-injection cases provide EvidencePacks, not run bundles. | confirmed | R1/R2 demo traces | Trace rule findings to metric evidence and mark canonical/source links unavailable. |
| Standalone demonstration must not depend on Randy or SUMO. | User explicitly requested a self-contained standalone product phase without real integration. | confirmed | Synthetic generator, demo workspace, reports, UI launch | Generate standard synthetic bundles and route them through the existing import-first pipeline. |
| Synthetic generator outputs are software fixtures, not calibrated simulations. | `SyntheticScenarioConfig` and docs describe simple deterministic relationships only. | confirmed | Demo interpretation, viva claims | Label all generated artifacts synthetic and avoid real-world validation claims. |
| Synthetic policy profiles are not real algorithms. | Profiles are named `synthetic-*` and documented as workload-generation behavior. | confirmed | Scenario labels, R3 demo | Do not claim MAPPO/DQN/Lyapunov behavior from standalone profiles. |
| Standalone report export should prefer bundle paths. | Report commands build from validated bundles and do not require registry-run lookup. | confirmed | Report CLI | Use bundle paths for reproducible reports; keep registry-run shortcuts as future work. |
| GitHub Actions is an appropriate CI example for this repository. | A `.github/workflows/ci.yml` file now runs local package quality gates and standalone smoke checks without secrets. | inferred | CI/release | If hosting changes, keep the workflow as a removable example and document equivalent commands. |
| Product Polish is presentation and workflow only. | New pages call existing generator, registry, reporting, validation, metrics, diagnostics, provenance, and search services. | confirmed | Streamlit UX | Do not add new scientific capability in UX polish work. |
| A guided workflow can be added without a parallel analysis implementation. | `ui.guided` stores only stage metadata and the Streamlit page routes each stage to existing pages/services. | confirmed | First-use and mobile research UX | Keep the guide read-only and preserve explicit synthetic/imported boundaries. |
| Scenario Builder is synthetic-only. | It validates `SyntheticScenarioConfig` and writes standard generated bundles. | confirmed | Scenario authoring | Keep Randy/SUMO controls unavailable until real adapters exist. |
| Report Dashboard regeneration is explicit. | The Reports page calls reporting builders only after a user action. | confirmed | Reporting workflow | Never regenerate reports automatically on page load. |
| Settings are session-scoped. | Settings are stored in Streamlit session state only. | confirmed | Local UX preferences | Do not imply authentication or persistent user profiles. |
| TOS campaign comparisons can be paired by fleet seed. | The evaluation master contains campaign, cell, evaluation fleet, and fleet seed for every row; the supplied matrix has common seeds 0-4. | confirmed | Paired TOS comparison | Pair only exact common keys and report unmatched seeds and compatibility differences. |
| TOS training warm-up `nan` values are valid unavailable observations. | Every inspected training CSV uses initial non-finite measured fields while step/time fields remain finite. | confirmed | Training explorer and JSON export | Convert measured non-finite fields to null and never emit JSON NaN. |
| Generalisation domain can be inferred from performance. | Domain relationship comes from package dictionary/training records, not observed score. | contradicted | Generalisation matrix | Use explicit source evidence only; retain unknown otherwise. |
| The static TOS atlas may be publicly deployed now. | Technical output is self-contained, but permission to publish source-derived aggregate values is not recorded. | unknown | Static hosting and dissertation sharing | Generate locally with a permission notice; do not deploy publicly until permission is confirmed. |
| Experiment-level EvidencePacks may infer training-validation pairs. | Run labels and ordering do not establish a scientifically matched pair. | contradicted | R5 and experiment analysis | Accept only caller-declared pairs with metric, policy, seed, and provenance available for review. |
| The R4 and R5 default thresholds are externally calibrated. | The new configs are deterministic engineering defaults exercised on synthetic tests only. | contradicted | Diagnostic interpretation | Label thresholds provisional; require representative evidence and expert review before real claims. |
| S5 and S6 are calibrated Manchester simulations. | Both presets are generated by the existing deterministic synthetic fixture model and use synthetic identifiers. | contradicted | Event/siting and road-clearing demonstrations | Present them only as workflow fixtures; do not claim SUMO, Manchester, traffic-engineering, or policy validity. |
| The default portfolio is a trained or validated selector. | It is an ordered, inspectable ruleset over declared synthetic seed features. | contradicted | Portfolio prototype | Expose matched rules and regret; call it synthetic and avoid performance or deployment claims. |
| Manual protocol tracking implies execution. | Tracking records only planned/received/validated/matched/rejected/complete states in SQLite. | contradicted | Experiment Manager and CLI | Keep launch capability false and require an external executor plus normal validation/import. |
| Draft evaluation materials constitute ethics approval. | `docs/evaluation/` explicitly marks every artifact as a draft that is not submitted or approved. | contradicted | Participant evaluation | Do not recruit or collect data until institutional approval and supervisor confirmation are recorded. |
| Incident/event authoring controls Randy/VEC or SUMO. | Scenario Builder writes the synthetic configuration and generated incident rows only. | contradicted | Scenario authoring | Keep external execution controls unavailable; preserve authored parameters as synthetic provenance. |
| Registering an experiment plan should create executable runs. | The planner validates only the existing `Experiment` model and persists status `planned`; no execution adapter is selected. | contradicted | Experiment Planner, registry | Preview run slots but create no `Run` records and expose no launch control. |
| Common-random-seed planning can use registered seed metadata without simulator access. | The `Experiment` model already stores baseline/variation seeds, policy labels, and a common random-seed set. | confirmed | Experiment Planner, reproducibility | Validate references against the registry and preserve labels as planning metadata only. |
| Suggested protocol run and bundle IDs prove that an external producer executed the planned configuration. | The exporter creates deterministic coordination identifiers before external execution and cannot observe producer behavior. | contradicted | Experiment protocol matching | Treat IDs as suggestions; require core manifest metadata and normal bundle validation, and retain scientific-validation limitations. |
| A planned checkpoint can be inferred when the experiment policy label differs from the seed policy label. | No adapter or registry evidence maps arbitrary policy labels to checkpoints. | contradicted | Protocol slots and bundle matching | Copy and require a checkpoint only for an exact seed-policy label match; otherwise emit a warning and leave it unresolved. |
| R5 gaps can be averaged across mixed policies, checkpoints, metric contracts, or role-specific environments. | The same rule result would then combine scientifically different contrasts. | contradicted | Experiment evidence and R5 | Reject mixed pair sets; require unique common random seeds and explicit compatible provenance before aggregation. |
| A winner can be ranked after only warning about mixed units or metric/environment versions. | Numeric order is not meaningful across incompatible contracts. | contradicted | Winner map | Exclude the affected seed family and retain a machine-readable warning. |
| The synthetic held-out portfolio study demonstrates external selector performance. | The fixture uses five deterministic synthetic seed families, synthetic policy profiles, and a fixed hand-written selector. | contradicted | Portfolio study and dissertation claims | Use it only to verify the evaluation workflow; require real policies, calibration, statistics, and external evidence for performance claims. |
| Authored incident fields may be dropped between config, seed, CSV, and canonical evidence. | Round-trip tests cover type, location, severity, timestamp, duration, lanes, demand multiplier, and vehicle references. | contradicted | Scenario authoring and generic CSV | Preserve every supported field and keep incident-seeded variants explicitly synthetic. |
| The bundled participant-result example is collected participant evidence. | `docs/evaluation/mock_results.json` requires `dataset_mode=synthetic_mock`, and the analyser rejects other modes. | contradicted | Participant evaluation | Use it only to verify software; exclude withdrawn records and require ethics approval before real collection. |
| The constrained findings renderer is an LLM or can create new diagnoses. | The renderer contains deterministic templates and copies fields from an existing `DiagnosticReport`. | contradicted | Diagnostic communication | Cite finding IDs/evidence keys and never calculate metrics, invent findings, or strengthen causal language. |
| Vehicle x/y replay is a geographic Manchester map. | The figure plots only source coordinates and labels both axes non-geographic. | contradicted | Replay UI | Show incident locations as text metadata unless coordinate evidence exists; show unavailable when x/y is absent. |
| Missing temporal windows may be skipped or treated as zero. | The complete window grid distinguishes unavailable, excluded, partial, low-coverage, and eligible observations. | contradicted | DIA-01 temporal evidence and R6 | Preserve every ordinal; ineligible observations break consecutive baseline, degradation, and recovery runs. |
| A degradation event may be inferred from the first visible metric change. | A time-series change does not establish an intervention or incident timestamp. | contradicted | R6 event recovery | Accept only an optional researcher-declared event and map it to its containing half-open effective window. |
| One R6 threshold is calibrated across every metric and unit. | R6 applies an absolute adverse delta in the selected metric's own unit; defaults are synthetic-development values only. | contradicted | R6 interpretation | Record the exact metric/unit/config, label defaults provisional, and calibrate per metric and study before external claims. |
| A high energy column or TOS aggregate is sufficient for R8. | R8 admits only exact canonical v1.0 per-completed-task energy, complete coverage, matching counts, `J/task`, and its completed-task denominator. | contradicted | DIA-03 energy diagnosis | Keep mixed-unit, partial, source-specific, and contract-incompatible energy insufficient; never relabel joules per arrival. |
| The R8 default is a real-world energy-efficiency standard. | The `1.50 J/task` boundary and support of 10 separate labelled synthetic development cases only. | contradicted | R8 interpretation | Label the configuration provisional and require predeclared, representative calibration before external claims. |
| Every deterministic diagnostic rule has a meaningful scalar nearest flip. | R0 has no severity boundary; R1-R4 and R6 have compound or unlike-unit configuration paths; arbitrary declarative rules lack a v1 monotonicity contract. | contradicted | DIA-05 sensitivity interpretation | Support only the exact single inclusive boundary for R5/R7/R8 and report all other families unsupported. |
| A nearest-flip threshold is a recommended or calibrated replacement default. | `NearestFlipAnalysis` describes the exact observed boundary and verifies it in memory without persisting configuration; it is selected after seeing the evidence. | contradicted | Rule configuration, dissertation claims, DIA-06 | Call it descriptive sensitivity, retain provisional defaults, and require a predeclared calibration study before external use. |
| An interactive sweep may show only triggered or transition points. | Selective retention would conceal instability, conflicts, and insufficiency across the declared range. | contradicted | DIA-06 report and UI | Retain and render every evaluated point and ordinary status; fingerprint the full sequence. |
| Changing a DIA-06 control updates a diagnostic default. | Range/rule/point selectors are exploratory inputs and the service returns a new immutable artifact. | contradicted | DIA-06 UI/configuration | Keep exploration session-only; require explicit complete RuleSetConfig download/import and never write registry/package/source state. |
| Simultaneously triggered rules automatically conflict or corroborate each other. | Co-occurrence and similar wording do not define a scientific relationship. DIA-07 v1 admits only three static policies with exact activation gates. | contradicted | DIA-07 interpretation | Emit no relationship for undeclared pairs; require an ADR, explicit statuses, overlap basis, and tests for each new policy. |
| Cross-rule suppression removes or changes a lower-priority RuleResult. | ADR-027 defines suppression as presentation/actionability metadata only; every original result, status, reason, evidence, and fingerprint is retained. | contradicted | DIA-07 reports and UI | Render relationships separately, list suppressed IDs, and show/download every unchanged original result. |
| Common-seed replicate results may be analysed as independent unpaired samples. | The registered experiment declares the same random-seed units for baseline and variation; ADR-028 preserves those pairs in estimation, resampling, testing, and effects. | contradicted | STA-01 statistical study | Pair only exact random seeds and use the original-unit mean variation-minus-baseline difference. |
| Missing, duplicate, or contract-incompatible replicates may be silently removed to recover a result. | STA-01 records every expected seed, collection fingerprint, exclusion, duplicate, and compatibility signature. | contradicted | STA-01 pairing and interpretation | Retain exclusions, require at least three compatible pairs, and return insufficient/incompatible when the declared study cannot be evaluated. |
| Ordinary non-significance demonstrates equivalence or no practical difference. | The STA-01 two-sided sign-flip p-value addresses a sharp zero-effect null; STA-03 separately requires a predeclared margin and both one-sided TOST rejections. | contradicted | STA-01/STA-03 dissertation claims | Never infer equivalence from STA-01. Report the STA-03 margin, basis, both tests, interval, assumptions, and limitations separately. |
| Synthetic known-distribution tests validate a real policy or Manchester deployment. | Constructed fixtures pin the method implementation only and carry `synthetic=true`. | contradicted | STA-01 acceptance evidence | Use synthetic results as software/method verification and require compatible real experiments for external claims. |
| N-way policies may be ranked using different available random-seed denominators. | ADR-029 admits a seed only when every selected policy has exactly one compatible endpoint. | contradicted | STA-02 ranking estimand | Exclude an incomplete seed from every policy denominator and retain the missingness audit. |
| Overlapping bootstrap intervals define a policy tie or equivalence. | STA-02 ties use only the predeclared absolute numerical tolerance; STA-03 requires a separate margin and paired TOST. | contradicted | STA-02/STA-03 interpretation | Keep winner-map ties and interval overlap separate from the predeclared STA-03 equivalence decision. |
| The policy with the highest bootstrap top-rank frequency is causally or universally best. | Rank frequency is the empirical stability of this metric and admitted seed sample under joint resampling. | contradicted | STA-02 dissertation claims | Report metric, objective, cohort, missingness, method, source mode, and limitations with every ranking. |
| TrafficTwin can supply one defensible equivalence margin for every metric. | Completion fractions, milliseconds, joules per task, and fairness gaps have different units and practical meanings; no universal threshold is evidenced. | contradicted | STA-03 analysis plan | Require a positive original-unit margin, basis, written justification, and reference for a literature claim before evaluation. |
| Failed TOST proves that conditions are meaningfully different. | A failed intersection-union decision may reflect an outside-margin mean, inadequate precision, or thin/noisy evidence. | contradicted | STA-03 interpretation | Use `equivalence_not_demonstrated`; do not relabel it as difference, superiority, non-inferiority, or deployment significance. |
| A zero-variance paired sample can safely produce infinite TOST statistics. | Student-t standard error and finite test statistics are undefined at zero paired-difference variance. | contradicted | STA-03 availability | Return a typed `degenerate` artifact with no fabricated statistic or p-value. |
| A newly generated golden may immediately approve its own current output. | Self-acceptance would turn any current result into a pass without an independent versioned decision. | contradicted | STA-04 golden lifecycle | Generate `candidate` by default; require an explicit approver label and acceptance note before pass/fail evaluation. |
| Missing or context-incompatible regression evidence is a numerical failure. | No valid expected-versus-actual scalar comparison exists when the required target, unit/version, source identity, or typed context is unresolved. | contradicted | STA-04 CI interpretation | Return `unavailable` and retain blocking/check reason codes; reserve `failed` for complete finite scalars outside tolerance. |
| Relative tolerance alone defines movement around an expected zero. | The versioned relative component is `relative_tolerance × abs(expected)` and is therefore zero at an expected zero. | contradicted | STA-04 tolerance planning | Declare a non-zero absolute tolerance when movement around zero is acceptable. |
| A passing regression gate proves scientific equivalence or policy quality. | STA-04 checks only selected accepted scalar expectations; it is neither TOST nor external validation. | contradicted | STA-04 reporting and dissertation claims | Report ignored fields, source policy, tolerances, and limitations; use STA-03 for a predeclared equivalence claim. |
| Observed post-hoc power adds independent evidence about a completed STA-01 result. | Under a fixed model, observed power is a transformation of the observed effect/test and encourages result-dependent interpretation. STA-05 deliberately requires prospective researcher-declared inputs. | contradicted | STA-05 planning and dissertation claims | Use the helper before confirmatory results; never relabel its output as retrospective evidence that a completed study was adequately powered. |
| The STA-05 normal approximation is exact power for the STA-01 sign-flip test. | Sign-flip power depends on a complete alternative distribution, while STA-05 v1.0 uses only a target effect and paired-difference variance under a normal paired-mean approximation. | contradicted | STA-01/STA-05 method interpretation | State the normal/fixed-variance assumptions and do not transfer the result to sign-flip, TOST, N-way, or multiplicity-adjusted inference. |
| A synthetic, provisional, or small-pilot variance provides externally validated sample-size calibration. | These bases describe method or early design evidence; variance uncertainty and domain transfer are not propagated by version 1.0. | contradicted | STA-05 input provenance | Retain the machine labels and warnings, report pilot size/bases, and obtain defensible study-specific inputs before external claims. |
| A required pair count guarantees achieved power or complete usable runs. | The calculation assumes the declared effect/variance are correct and every common seed yields one compatible baseline and variation run without attrition. | contradicted | STA-05 planning and execution | Call it a planning estimate, keep assumptions visible, and separately plan or justify any missingness/attrition allowance. |
| Every compatible metric difference has meaningful additive row weights. | Percentiles, extrema, distinct counts, state-machine metrics, grouped aggregates, and arbitrary plugin outputs do not generally equal a sum of independent row terms under their ordinary definition. | contradicted | PRO-01 difference provenance | Use the closed ADR-033 arithmetic registry; expose eligible lineage with null weights for compatible non-decomposable scalar metrics. |
| A reconciled arithmetic difference identifies what caused the outcome change. | Reconciliation proves only that accepted-row calculation terms sum to the ordinary variation-minus-baseline metric delta. It is not an intervention, counterfactual, or causal identification design. | contradicted | PRO-01 interpretation and dissertation claims | Preserve the mandatory JSON/CSV non-causality statement and use contributor, lineage, and arithmetic-contribution language only. |
| Baseline and variation rows must share identifiers before their aggregate difference can be decomposed. | Each run aggregate is decomposed using its own eligible rows and denominator; subtracting the two sums requires no invented row matching. | contradicted | PRO-01 arithmetic | Apply negative signs to baseline run terms and positive signs to variation run terms; retain source IDs as lineage, not pairing keys. |
| A provenance graph may render every trace node without a declared bound. | v0.5 requires bounded graph views, and unbounded rendering can hide omission policy or overwhelm the local UI. ADR-034 publishes deterministic traversal plus exact retained/omitted counts. | contradicted | PRO-02 graph export and explorer | Enforce default and hard node/edge limits, retain the root, and show exact truncation metadata. |
| Removing absolute paths makes a graph export anonymous. | Path redaction prevents machine-path disclosure but ordinary run, vehicle, RSU, filename, and scalar attributes may remain identifying. | contradicted | PRO-02 disclosure and dissertation sharing | Default to path-safe export, provide structure-only disclosure, and require review before sharing private traces. |
| Graph renderer coordinates or layout are deterministic provenance evidence. | DOT/GraphML node and edge content is deterministic, but layout algorithms and renderer versions may place the same graph differently. | contradicted | PRO-02 interpretation | Compare stable graph/view fingerprints and typed content; treat layout as presentation only. |
| A provenance completeness denominator may omit unavailable report results. | Omitting unsupported claims would inflate the score and conceal missing evidence; ADR-035 fixes the denominator to every explicit typed claim in the selected report template. | contradicted | PRO-03 denominator | Retain unavailable claims with zero contribution and publish named non-claim exclusions. |
| Reaching one source row makes a whole report claim source-row complete. | Trace depth may come from a retained sample; completeness additionally requires a non-empty, reconciled ledger for every accepted canonical candidate and valid source locators. | contradicted | PRO-03 classification | Publish depth and ledger counts separately; require both for `source_row_complete`. |
| A provenance completeness score measures truth, causal validity, scientific importance, or raw-row acceptance. | The score is an equal-weight fraction over typed report claims and accepted-canonical-row lineage only. Rejected raw rows remain validation evidence outside the metric candidate population. | contradicted | PRO-03 interpretation | Report the template, denominator, exclusions, validation findings, class counts, and limitations with the score. |
| A parameter-sweep request proves that an external simulator executed every point. | EXP-01 external artifacts carry `not_executed`, false direct-launch support, and null launcher/command. | contradicted | EXP-01 external coordination | Require completed outputs to return through ordinary immutable validation/import before any result claim. |
| Any dotted configuration field is safe to sweep independently. | Coupled mixes, lists, placement, failures, incidents, identity, and arbitrary nested fields need dedicated invariants; ADR-036 admits only a closed scalar catalogue. | contradicted | EXP-01 request validation | Reject unknown/incompatible/repeated paths and validate every complete derived snapshot before writes. |
| Missing or mapping-valued metrics can be represented as zero in a response surface. | Zero would fabricate a numeric response and hide evidence/type availability. | contradicted | EXP-01 response rows | Retain metric status/reasons; admit only finite numeric values and mark other response values unavailable/invalid. |
| A local synthetic sweep is calibrated traffic/VEC evidence. | The only local executor is TrafficTwin's deterministic software-fixture generator plus ordinary validation/metrics. | contradicted | EXP-01 interpretation | Label every point synthetic/evaluation evidence and make no optimisation, causal, simulator-fidelity, or external-validity claim. |
| Raw imported evidence may be mutated in place for convenience. | EXP-02 admits only an ordinarily valid bundle explicitly labelled synthetic/evaluation and always writes a separate derived bundle. | contradicted | EXP-02 admission and materialisation | Reject imported/raw evidence and inferred/canonicalised manifests; preserve the parent and its files unchanged. |
| Removing an RSU permits TrafficTwin to invent task rerouting or changed target IDs. | No evidenced routing policy or simulator transition is available at this boundary. | contradicted | EXP-02 RSU-removal semantics | Remove only exact matching infrastructure rows, update declared failure/count metadata where available, and retain task/target bytes unchanged. |
| A seeded mutation may depend on process-global pseudo-random state. | Interpreter, library, and call-order state would weaken replayability. ADR-037 derives choices and deltas from SHA-256, stable row identity, and the declared seed. | contradicted | EXP-02 deterministic operators | Pin request, parent, plan, and result fingerprints and test repeat execution byte-for-byte. |
| A mutation change ledger may be silently truncated for large inputs. | A partial ledger cannot support exact audit or reconstruction claims. | contradicted | EXP-02 bounded provenance | Reject a request before publication when more than 20,000 rows would change; retain every admitted row change. |
| EXP-02 transparently rewrites compressed CSV or Parquet mutation targets. | Encoding-preserving mutation semantics and byte-stable writers are not contracted in v1.0. | unsupported | EXP-02 target admission | Mutate only declared uncompressed CSV; copy other declared files unchanged and expose unsupported targets as errors. |
| EXP-03 bounded-uniform noise is a calibrated real sensor, network, or physical-error model. | No empirical distribution, correlation, bias, drift, hardware, radio, or packet-loss evidence is supplied; ADR-038 defines a deterministic software fixture only. | contradicted | EXP-03 interpretation and dissertation claims | Retain synthetic/not-calibrated labels and use the feature only for controlled software-robustness evaluation. |
| Measurement impairment may alter task outcomes, timestamps, routing, or incidents. | EXP-03 is restricted to selected generated observation fields and observation-row dropout; clean task/trip/incident tables are pinned by tests and golden identity. | contradicted | EXP-03 generator boundary | Leave tasks, trips, incidents, time, decisions, completion, latency, targets, routing, and energy unchanged. |
| A nominal EXP-03 dropout fraction is an independent packet-loss probability. | ADR-038 uses an exact bounded count selected by stable SHA-256 rank and retains at least one row. | contradicted | EXP-03 missingness interpretation | Report requested fraction plus exact before/dropped/retained counts; never describe it as empirical or Bernoulli packet loss. |
| Process-global randomness is sufficient for repeatable measurement noise. | Global state can change with call order and unrelated enabled fields. EXP-03 hashes the seed, table, field, original generated-row index, and clean row separately. | contradicted | EXP-03 reproducibility | Preserve the scenario seed, measurement seed, model/configuration fingerprint, and audit fingerprint. |
| An EXP-03 manifest audit may contain any re-fingerprinted field or table declaration. | The audit schema reconciles exactly the fields, units, clamps, bounds, dropout tables, fractions, and eligible-row counts implied by its strict configuration before fingerprint validation. | contradicted | EXP-03 provenance integrity | Reject missing, extra, duplicate, or semantically inconsistent audits through ordinary manifest validation. |
| A LaTeX or figure renderer may recompute, round into a new result, or reinterpret scientific values. | ADR-039 and the REP-01 projectors consume completed typed artifacts and carry their existing values, availability, method labels, confidence categories, and rule statuses into one renderer projection. | contradicted | REP-01 research exports | Keep metrics, comparisons, statistics, and diagnostics in their tested engines; the renderer only bounds, escapes, labels, and lays out the typed projection. |
| A positive or rightward static bar means that a policy or result is better. | Numeric REP-01 figures show signed source values on one linear presentation scale and have no metric-direction or objective inference. | contradicted | REP-01 figure interpretation | Display the non-favourability statement and interpret direction only from the metric/study contract. |
| A rule status or confidence category can be converted to a probability for plotting. | TrafficTwin has no calibrated probability model for deterministic rule status or confidence categories. | contradicted | REP-01 rule figures | Preserve exact categorical statuses and never plot a fabricated probability or severity score. |
| A generated LaTeX fragment is automatically submission-ready without review. | REP-01 deliberately leaves document class, typography, float placement, wide-table pagination, and final rendering to the dissertation environment. | contradicted | REP-01 dissertation use | Compile in the final document, visually inspect tables/figures, retain warnings/source mode, and make any layout edit explicit and reviewable. |
| A completed local VEC execution could be treated as scientific or reproduction evidence. | The accepted VEC-09 admission binds the audited source run, and VEC-08 grading needs a calibration pair and review; ADR-052 imports executions as structural evidence only. | contradicted | One-click execute-and-import records | Keep scientific admission `unavailable` with standing reason codes and literal no-relabelling guards in every imported record. |
| Streamlit session defaults may hard-code the registry path regardless of configuration. | The demo launcher sets `TRAFFICTWIN_REGISTRY_PATH`, and pages must honour it; the previous static default silently masked the configured workspace registry. | contradicted | UI configured-registry behaviour | Seed session state from `load_ui_config()` and never overwrite an intentional in-session selection; regression-tested. |
| A locally executed synthetic SUMO run could be presented as Manchester traffic or Randy evidence. | The preset scenario is original TrafficTwin-authored synthetic geometry, and ADR-053 pins literal synthetic/non-Manchester/non-Randy labels through the preset, manifest, receipt, and import record. | contradicted | Controlled SUMO execution claims | Keep the literal labels in every artifact and never map summary occupancy to canonical traffic counts. |
| Possessing the controlled SUMO runner code implies the execution capability is accepted. | Code presence alone was insufficient; the official Eclipse SUMO 1.27.1 arm64 macOS package now passes the real execution, validation, and import acceptance test. | contradicted | Controlled SUMO execution status | Keep `controlled_sumo_execution` conditional on each request-specific preflight and retain the real-runtime acceptance test as the installation gate. |
| Public DfT, WebTRIS, TfGM, BODS, and National Highways operational sources collectively provide complete live Manchester road telemetry. | The sources have different update cycles, road/modal scopes, and semantics: DfT counts are historical surveys, WebTRIS covers strategic roads, TfGM signals are infrastructure, BODS positions are buses, and the current National Highways REST products are Strategic Road Network events/restrictions/sign status rather than continuous traffic telemetry. | contradicted | `MAN-02`–`MAN-08`, product wording, dissertation scope | Keep every source and coverage boundary visible; label continuous city-road flow/measured-speed coverage unavailable until an authorised contract proves it. |
| WebTRIS response bodies can always be parsed directly as identity JSON. | The controlled 23 July 2026 service response used `Content-Encoding: gzip`; its raw wire bytes failed strict JSON parsing until bounded decoding produced a separately hashed parser payload. | contradicted | `MAN-03` acquisition, accepted replay, charts, map scene | Preserve the encoded bytes, apply only the audited bounded gzip decoder, bind both hashes, and reject unaudited encodings. |
| Whitespace in the TfGM archive attribution can be normalised without changing the frozen evidence contract. | The pinned official ZIP contains two spaces between its attribution sentences; the earlier one-space literal rejected the exact audited archive despite matching ZIP and CSV hashes. | contradicted | `MAN-04`, layer attribution, receipts | Retain the exact archive wording in source receipts and map manifests; presentation may wrap lines but must not silently rewrite the retained literal. |
| A generic accepted snapshot always contains enough source-specific scope to reproduce its parser report without the original acquisition receipt. | Historical WebTRIS daily-quality bodies contain no site name, while the current parser report fingerprint includes the caller-supplied name and the generic request manifest records only transmitted query parameters. | contradicted | `MAN-01`, `MAN-03`, receipt-free accepted replay | Inventory and hash-verify the quality snapshot but mark parser scope unavailable; never invent a name. Consider a separate reviewed source-specific receipt for future acquisitions. |
| A recently downloaded source record is live. | Retrieval time does not establish observation time or source publication latency. | contradicted | `MAN-01`, `MAN-07`, Manchester Operations | Compute source-specific freshness from observed time and policy; show stale/unavailable when an exact live claim cannot be supported. |
| Querying WebTRIS for the previous day can turn it into a near-live road feed. | WebTRIS officially describes publication as normally about one month in arrears; on 24 July 2026 the 23 July daily request returned HTTP 204, while 24 June returned the complete accepted 96-interval day. | contradicted | `MAN-03`, `MAN-07`, live-road product wording | Keep WebTRIS historical/latest-available. Use the separately bounded current National Highways operational REST products only for their closure/restriction/VMS meanings. |
| The current National Highways operational products require a public NTIS callback receiver. | The developer catalogue exposes key-authenticated HTTPS REST paths for closures, speed-managed areas, and digital VMS; all three passed an isolated real-source pull on 24 July 2026. | contradicted | `MAN-01`, operational acquisition and deployment | Keep the v1 workflow as explicit bounded pulls with no public ingress, webhook, daemon, or hidden scheduler. |
| A National Highways temporary speed restriction is a measured vehicle-speed observation. | Speed Managed Areas reports an imposed temporary limit, including a documented suspended/zero case; it does not measure traffic speed or volume. | contradicted | `MAN-07`, maps, comparisons, dissertation wording | Preserve the `temporary_speed_limit_kph` name and keep measured speed, flow, density, and congestion structurally unavailable. |
| A broad Manchester latitude/longitude envelope is an official administrative boundary. | The v1 envelope is a caller-declared source filter and differs from the separately packaged, hash-verified ONS Manchester and Greater Manchester display boundaries. | contradicted | `MAN-07`, `MAN-08`, map coverage | Label it as a broad operational study envelope, retain `official_boundary=false`, use ONS polygons for display context only, and do not claim city-wide completeness. |
| Retrieval time or a successful request proves that National Highways operational data is live. | The responses carry source `publicationTime`; the provider has not supplied a ten-minute SLA. | contradicted | `MAN-07` freshness | Use `publicationTime`, apply the versioned self-imposed ten-minute `near_live` ceiling, and show older/outage-cached evidence as stale. |
| A bus position is a general road-traffic count or speed observation. | BODS SIRI-VM represents transit vehicles/services, not private-vehicle flow or complete traffic conditions. | contradicted | `MAN-05`, map layers, metrics | Emit a separate live-transit record and retain bus/transit labels in every view and export. |
| An observed count can be converted directly into SUMO vehicle demand. | Site/edge scope, interval, direction, vehicle classes, sampling design, network mapping, and calibration uncertainty must be reconciled first. | contradicted | `MAN-09`, controlled SUMO baseline | Require accepted network binding, map matching, temporal profile, calibration, residuals, and analyst confirmation; otherwise keep the baseline unavailable. |
| A public endpoint automatically permits raw redistribution. | Access, use, retention, attribution, derivative-publication, and Randy/private permissions are distinct. | contradicted | `MAN-01`–`MAN-06`, public fixtures, hosting | Record licence/terms per snapshot and publish only artifacts whose manifest explicitly permits the target use. |
| v0.7 may migrate a valuable v0.6 workspace in place. | The immutable v0.6 release must remain independently reproducible, and in-place mutation would weaken rollback and comparison. | contradicted | `REL-01`, registry/cache/workspace evolution | Use separate workspaces and copy-on-write migration with preview, backup, verification, rollback, and source-byte preservation. |
| v0.7 must add absolute time and Manchester metadata to the canonical `TrafficObservationRecord`. | The existing canonical schema is stable and adequate for downstream elapsed-time evidence; source-specific Manchester records can retain absolute time without widening every current consumer. | contradicted | `MAN-02`–`MAN-07`, canonical projection | Keep the canonical schema unchanged and use a versioned `ManchesterTimeBasis` with an explicit UTC analysis-window anchor to derive `timestamp_s`. |
| A partially migrated page set may replace the complete v0.6 navigation while v0.7 UI work continues. | Partial replacement can hide working research capabilities and makes rollback/page parity ambiguous. | contradicted | `UX-01`, navigation migration | Maintain the full v0.6 navigation until all 34 existing pages have a tested v0.7 mapping, then switch atomically. |
| A BODS feed or display-name substring is sufficient to identify Bee Network services. | Branding and free-text names are not stable service identifiers. Gate A accepts identifier-only NOC matching; one accepted live snapshot verifies five codes while `BNVB` remains pending. | contradicted | `MAN-05`, Bee Network filtering | Use the fingerprinted policy-v1 exact allowlist and three-valued accounting; retain pending/non-matching services explicitly, let later accepted evidence open a review only, and never fall back to names, geography, or automatic policy activation. |
| Live BODS snapshots can accumulate indefinitely because parsed vehicle IDs are pseudonymised. | Immutable accepted and quarantine raw bytes can retain source `VehicleRef` values; pseudonymised parser output does not remove that private raw-data risk. | contradicted | `MAN-05`, private workspace retention | Preview a bounded complete-family cleanup, protect active/newest evidence, require exact confirmation, and keep legal approval plus secure-erasure claims unavailable. |
| A live Streamlit page should poll BODS or National Highways on each rerun or timer tick. | UI reruns are not an acquisition scheduler and BODS privacy/retention remains unresolved. Phase 188 separately authorises one explicit process-level National Highways worker at three calls per five minutes, with truthful receipts and stale fallback. | partly superseded | `MAN-05`, `MAN-08`, Manchester Operations | Keep BODS operator-triggered. Keep UI reruns local. Permit only the fixed National Highways server worker plus manual fallback, one OS-locked execution at a time, at least 60 seconds between attempts, exactly three calls per refresh, and aggregate-only history. |
| Every imported or generated `x`/`y` coordinate is suitable for the Manchester map. | Coordinates may be source-local, generated, use an unknown CRS, or lack an accepted transformation and uncertainty contract. | contradicted | `MAN-08`, map and replay layers | Require versioned spatial admission for every layer; keep unknown or unadmitted coordinates off the geographic map while allowing clearly labelled non-geographic replay. |
| The existing `streamlit>=1.36` floor is sufficient for the v0.7 product shell. | The approved shell needs later APIs. Packaging now declares `streamlit>=1.58,<2`, the candidate 45-test navigation suite passes `streamlit==1.58.0`, and the development lock remains `1.59.2`; the new Manchester page still needs its minimum-version rerun. | contradicted | `UX-01`–`UX-03`, packaging and UI CI | Retain the reviewed floor and exact lock, and rerun every new UI slice on both versions before acceptance. |
| Passing the v0.7 Gate-A source audit completes `MAN-01`. | An audit freezes the allowed contract but does not prove that the bounded transport, parser, immutable snapshot, and receipt service works. | contradicted | `MAN-01`, Gates A and B | Keep `MAN-01` planned after Gate A and accept it only when the Gate-B acquisition implementation also passes. |
| A structurally valid registry proves that it was produced by the tagged `v0.6.0` release. | The frozen v0.6 SQLite schema records schema migrations but not a verifiable producing package version or checkout identity. | contradicted | `REL-01` compatibility admission | Keep `source_product_version` unknown until an immutable workspace marker, release manifest, or clean-checkout attestation supplies that evidence. |
| Copying the main SQLite file is safe while WAL or rollback-journal sidecars exist. | Uncheckpointed state may live outside the main file, so a main-file-only copy could be incomplete or inconsistent. | contradicted | `REL-01` compatibility copy | Require a closed, checkpointed, non-symlinked source and refuse `-wal`, `-shm`, and `-journal` sidecars before and after copying. |
| A byte-exact v0.6 registry copy can become the active v0.7 registry automatically. | Byte identity proves copying only; it does not prove producer release, schema migration, stored-payload compatibility, backup, activation, or rollback safety. | contradicted | `REL-01` migration and activation | Publish copies only in a non-active compatibility directory and require later reviewed migration/activation evidence. |
| Creating 34 `st.Page` scripts is sufficient evidence for the v0.7 navigation cutover. | File presence and AppTest rendering alone do not prove browser URL/history behaviour, cross-page state, package inclusion, accessibility, mobile layout, or minimum-version support. The later 35-route desktop/mobile light/dark audit closes the bounded automated browser slice only. | contradicted | `UX-01` atomic navigation migration | Keep the complete legacy router available through the explicit compatibility setting until remaining minimum-version, manual accessibility, participant, and final release gates pass. |
| A `netconvert`-produced SUMO network is byte-reproducible across runs over identical inputs. | Four SUMO 1.27.1 runs over identical real Manchester OSM input produced four different raw SHA-256 digests; the differences are the generation-banner timestamp and the echoed output filename. | contradicted | `MAN-09` network binding, reproducibility claims | Record both a raw digest and a canonical identity digest computed with XML comments removed, claim reproducibility only for the canonical digest, and set `byte_reproducible=false`. |
| `netconvert` 1.27.1 can build a network directly from an OpenStreetMap PBF extract. | The reviewed build reads OSM XML only and exits 1 on the pinned `.osm.pbf` with an XML parse error; its build features list no PBF reader, and no approved decoder is installed. | contradicted | `MAN-09` Greater Manchester network build | Sniff PBF framing and refuse with `OSM_PBF_DECODE_UNAVAILABLE` naming the missing decode step; keep the full Greater Manchester build blocked rather than silently broken. |
| A provider's `-latest` extract alias is a stable pin for a reproducible baseline. | `greater-manchester-latest.osm.pbf` responds `302 Found` and redirects to a dated file, so the alias content changes over time and the zero-redirect transport refuses it. | contradicted | `MAN-09` source pinning | Pin the dated provider file and record the retrieval date and the extract's own data-cutoff date separately. |
| The packaged ONS Manchester boundary polygons can serve as the scientific clipping boundary for an OSM extract. | They are BGC-generalised to 20 m, coastline clipped, and rounded to four decimal degrees, and `boundary_reference.py` already declares `scientific_clipping_available=false`. | contradicted | `MAN-09` extract envelope | Derive a labelled `derived_from_display_geometry` envelope with a declared margin and boundary uncertainty, and keep `scientific_clipping_boundary=false`. |
| Building a Greater Manchester network means DfT calibration evidence covers it. | DfT count points are published for Manchester local authority only, which is partial coverage inside the Greater Manchester baseline. | contradicted | `MAN-09`, `MAN-02`, calibration coverage | Classify locations outside the Manchester filter as `uncovered`/`unavailable`, keep `uncovered_is_zero=false`, and never substitute zero for a missing survey point. |
| A `netconvert` build that reproduces its canonical identity on a small network will reproduce it at city-region scale. | Four city-centre runs matched exactly, but one of three Greater Manchester builds differed in 238 of 10,913,444 canonical lines, all `<roundabout>` membership, with structural counts identical every time. | contradicted | `MAN-09` reproducibility claims, dissertation evidence | Never assert reproducibility from one build or from a smaller case; record `semantic_reproducibility=not_verified` and establish it only by comparing two real builds. |
| `origBoundary` in a produced SUMO network describes the network's extent. | It is the bounding box of everything `netconvert` read. Because an OSM extract retains whole ways crossing its edge, the Greater Manchester `origBoundary` reached longitude +1.46 while the network stopped at −1.88. | contradicted | `MAN-09` extent, coverage claims, containment tests | Compute the extent from `convBoundary` offset by `netOffset` and transformed through the network's own `projParameter`. |
| A tool's output file contains only the data it was asked to produce. | `netconvert` echoes its resolved configuration into a comment banner, so absolute input and staging paths were embedded inside the `.net.xml` and would ship with any published derived network. | contradicted | `MAN-09` publication, privacy | Link the input into staging and run on bare filenames, then remove the link before promotion; assert zero private paths in the produced network. |
| A gigabyte-scale artifact can be validated by reading it into memory. | The 1.25 GB Greater Manchester network would have peaked at several gigabytes once the canonical digest built a second copy. | contradicted | `MAN-09` validation at real scale | Stream digests, structure counts, and the projection prefix; validating the real network then peaks at 148 MB RSS. |
