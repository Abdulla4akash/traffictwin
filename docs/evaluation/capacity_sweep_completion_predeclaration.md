# Five-Regime Completion + Deep-Squeeze Onset — Exploratory Predeclaration

**Status: exploratory predeclaration under the standing in-session owner delegation
("run some more experiments", 28 July 2026). Approval recorded as relayed delegation,
never an owner-typed signature. Label ceiling `owner_approved_candidate`; every outcome
is publishable with equal prominence.**

- Predeclared: 28 July 2026, before any cell executed
- Context: the three-trace grid found capacity completely inert off-saturation and
  bracketed the outcome-sensitivity threshold between 175 and 2,488 concurrent slots.
  The weekday peaks were then probed and admitted (ADR-066) and measured LOW density
  (215 / 163 slots) — so this design (a) completes the five-regime sweep and (b) forces
  a low-density regime toward binding with a deep squeeze.

## Leg 1 + 2 — five-regime completion (`wd_am`, `wd_pm`)

Grid design unchanged (arms {2.5, 1.5, 1.0, 0.75}, seeds {50, 51, 52}, ukfleettrain
actor, 12 cells per leg). **Predictions, recorded now:** per-seed decision invariance
and complete outcome inertness (all arms identical), as in the weekend/event legs —
both peaks sit in the same density band. Any cross-arm variation is a published
localisation finding.

## Leg 3 — deep-squeeze onset (`ev-deep`)

The event-night trace with the squeeze extended far beyond the studied grid:
arms **cap-2.5 (baseline), cap-0.5, cap-0.25, cap-0.1**, same seeds {50, 51, 52},
12 cells. **Predictions, recorded now:** (i) decisions remain invariant at every level
(the policy cannot see capacity at any setting); (ii) outcomes remain inert until some
level binds, after which latency rises (the direction *opposite* to the collapse-hour
pattern is possible once capacity truly starves service — whichever direction is
measured is reported); (iii) the onset level, if any exists above cap-0.1, is the
measured product of this leg. If nothing binds even at cap-0.1, that bound is the
finding.

## Shared execution terms

Same instrument, budgets, ceiling (7,200 s), registry `.demo/registry-capacity-grid.sqlite`,
fresh-run admission per cell, exploratory analysis harness per leg; held-out seeds
untouched (`held_out_authorised = False`); nothing pooled across studies; campaign
approvals bind this document's committed SHA-256.

**Sign-off (relayed delegation, recorded by the agent — not an owner-typed signature):**
defaults above resolved under the standing delegation; the owner may halt at any time.
