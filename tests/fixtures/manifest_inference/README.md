# Synthetic manifest-inference fixtures

`value_patterns` tests deterministic header aliases, bounded value-pattern suggestions, explicit
confirmation, and application to a complete metadata template. `ambiguous` deliberately supports
multiple file kinds equally and must never be resolved without an explicit user edit.

Both fixtures are synthetic software-test evidence. They are not Randy/VEC, SUMO, Manchester, or
participant data.
